# Tiqian-rs — Agent 指南

本文档面向 AI coding agent 与开发者。`tiqian-rs` 是 [提椠 Tíqiàn](https://github.com/tiqian-cjk/tiqian) 文字排版核心的 Rust 移植。

---

## 项目速览

（待编写）

详细现状优先参考：

- `docs/tracking.md`：跟踪与上游 tiqian 的同步情况。包含同步策略、变更记录等。
- `docs/key-differences.md`：记录与上游 tiqian 的主要差异，包括设计、实现和行为上的不同。
- `docs/adr/*.md`：架构决策记录，通常记录了某个与上游差异相关的重要设计决策。
- `docs/iteration/*.md`：历史迭代记录。

---

## 通用编码规范

- **最小化改动**：实现新功能或修 bug 时，只改必要部分，不顺手重构无关代码。
- **从根本上解决问题**：拒绝补丁式修复、不叠床架屋；分析根本原因后再设计解决方案。
- **不确定就提问**：需要较大重构、需求不明、有歧义时，停下来用提问工具确认，不要自行猜方案。
- **拒绝矛盾需求**：需求互相冲突时，拒绝修改并给出详细解释，由用户决定取舍。
- **先读再写**：修改前先阅读相关代码，避免重复内容或与既有设计冲突。
- **性能意识**：尽量使用 O(1)、O(n) 等低复杂度的算法，不要出现 O(n^2) 或更高复杂度的操作，避免不必要的性能开销。
- **注释一律中文**：代码文件中的注释必须中文书写。
- **对话用用户偏好语言**：与用户的自然语言交流默认用中文。
- **当前处于开发阶段**：不需考虑向后兼容或用户数据迁移，但仍要关注可维护性与可扩展性。

### 任务执行流程

1. 接到多件事情时，先整体 review + 调研；有疑问先用提问工具询问，再开始实施。
2. 按顺序逐项实施（除非互相关联），可自行安排顺序。
3. 实施过程中如发现设计或计划有问题、有更好方案，提出讨论后再继续。
4. 每次实施完成后：
   - 检查编辑器报错并修复；
   - 自行判断是否需要更新 `docs/key-differences.md`、`docs/tracking.md`、`docs/adr/*.md`、`docs/iteration/*.md`，按需更新；迭代文档只应该更新当前迭代对应的文件，不要编辑历史记录。
   - 检查相关测试是否需要更新；
   - 再次 review，对照本规范与设计文档，检查是否有遗漏或违规。
5. 修改既有功能行为前，先确认改动范围与影响，必要时询问用户决策。

## 文档与规范驱动开发（DDD）

本仓库采用文档与规范驱动开发。稳定结论分别维护在：

- `docs/tracking.md`：跟踪与上游 tiqian 的同步情况。包含同步策略、变更记录等。
- `docs/key-differences.md`：记录与上游 tiqian 的主要差异，包括设计、实现和行为上的不同。
- `docs/adr/R<四位编号>-<name>.md`：架构决策记录，通常记录了某个与上游差异相关的重要设计决策。
- `docs/iteration/<date>-<name>.md`：历史迭代记录，某项新需求或较大修复的迭代记录。

### 工作流程

1. 接到新需求或多项任务时，先阅读持续维护文档、相关代码和现有测试。
2. 对新增特性或算法调整，先创建迭代文档，写清目标、范围、设计、兼容性、验证和回滚方案。
3. 按迭代文档实施；若实现中需要改变设计，先讨论确认，再更新迭代文档。
4. 完成后运行相关验证，并对照迭代文档或相关设计文档，检查是否符合预期。

小型、无行为变化的维护（例如明确的拼写修正）不应新建迭代文档，但仍需保持持续维护文档与实际状态一致。

---

## 测试、开发与发布

见 [Readme](README.md)。
