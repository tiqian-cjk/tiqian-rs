# 2026-08-31 2fae0df..59fca35 Kotlin 上游跟进

## 审计范围

| 项目 | 值 |
| --- | --- |
| Kotlin 审计区间 | `2fae0df461819932dc9ef0153b79be9ad0038959..59fca3597a072362c49ce1bade6401efc2d6063d` |
| Kotlin 提交数 | 598 |
| Rust 基线 | `c74b4410379f87742e0a290d401ca1401c7b6882` |
| 范围 | Kotlin `docs/` 与 `engine/src/` |
| 文件总数 | 277 |

本轮跨度较大。以下文件清单来自区间的完整 `git diff --name-status`，在阅读实现前建立；后续审计
按工作项推进并在本文件持续记录，不依赖对话上下文。

## 完整变更来源清单

本节仅按 Kotlin 路径记录审计输入，不表示同步或测试映射的完成状态。实际进度只记录在后文
“统一工作项状态”表；每个来源分组都可能向多个工作项提供证据。

### 来源 A：决策、架构与研究文档（40 个文件）

#### ADR（24 个文件）

- `docs/adr/0003-prefer-clreq-recommended-codepoints.md`
- `docs/adr/0004-punctuation-additive-glue-model.md`
- `docs/adr/0008-shaping-adapter-contract.md`
- `docs/adr/0009-autospace-policy.md`
- `docs/adr/0014-ink-bounds-calibrated-punctuation-geometry.md`
- `docs/adr/0016-android-textpaint-adapter.md`
- `docs/adr/0017-compose-desktop-renderer.md`
- `docs/adr/0020-tiered-push-in-compression.md`
- `docs/adr/0023-justification-as-baseline.md`
- `docs/adr/0030-rich-text-spans.md`
- `docs/adr/0039-web-rendering-path.md`
- `docs/adr/0040-build-time-web-font-snapshots.md`
- `docs/adr/0041-paragraph-dp-line-breaking.md`
- `docs/adr/0042-framework-web-integrations.md`
- `docs/adr/0046-core-text-shaping-adapter.md`
- `docs/adr/0047-core-text-rendering-frontend.md`
- `docs/adr/0048-suite-maven-and-package-namespaces.md`
- `docs/adr/0050-native-precompute-rust-bindings.md`
- `docs/adr/0052-precompute-cache-and-batch-renderer.md`
- `docs/adr/0053-web-prose-host-consolidation.md`
- `docs/adr/0054-measure-quantization-and-band-table.md`
- `docs/adr/0056-engine-module-and-platform-reorg.md`
- `docs/adr/0057-vertical-axis-exits-cache.md`
- `docs/adr/README.md`

#### 其他文档（7 个文件）

- `docs/architecture.md`
- `docs/clreq-punctuation-audit.md`
- `docs/contributing.md`
- `docs/roadmap.md`
- `docs/rust-unsafe-inventory.md`
- `docs/ts-port-assertion-checklist.md`
- `docs/web-prose-host-decomposition.md`

#### 研究记录（9 个文件）

- `docs/research/2026-08-20-harfbuzz-version-differential.md`
- `docs/research/2026-08-23-a5-metric-table-unification.md`
- `docs/research/2026-08-23-b10-engine-policy-abi.md`
- `docs/research/2026-08-23-precompute-dual-implementation-and-protocol-evaluation.md`
- `docs/research/2026-08-23-tshost-runtime-port-plan.md`
- `docs/research/2026-08-24-g-batch-execution-plan.md`
- `docs/research/2026-08-26-protocol-shared-split-and-haxe-kotlin-pilot-study.md`
- `docs/research/2026-08-29-engine-uncovered-lines.md`
- `docs/research/android-font-reports/2026-08-05-compose-font-selection-audit.md`

来源用途：记录既有取舍、架构边界和研究背景。

### 来源 B：排版核心实现（24 个文件）

- `engine/src/commonMain/kotlin/org/tiqian/clreq/ClreqProfile.kt`
- `engine/src/commonMain/kotlin/org/tiqian/core/UnicodeNumberData.kt`
- `engine/src/commonMain/kotlin/org/tiqian/core/UnicodeScriptEvidence.kt`
- `engine/src/commonMain/kotlin/org/tiqian/core/UnicodeWordCharacter.kt`
- `engine/src/commonMain/kotlin/org/tiqian/font/CjkDashCapabilityPolicy.kt`
- `engine/src/commonMain/kotlin/org/tiqian/font/FontPolicy.kt`
- `engine/src/commonMain/kotlin/org/tiqian/font/InlineShapingStylePolicy.kt`
- `engine/src/commonMain/kotlin/org/tiqian/layout/ClusterRoleResolution.kt`
- `engine/src/commonMain/kotlin/org/tiqian/layout/ContextualDashEllipsisRoleResolver.kt`
- `engine/src/commonMain/kotlin/org/tiqian/layout/ContextualPunctuationDisplaySubstitution.kt`
- `engine/src/commonMain/kotlin/org/tiqian/layout/ContextualQuoteRoleResolver.kt`
- `engine/src/commonMain/kotlin/org/tiqian/layout/LayoutDebugAssembly.kt`
- `engine/src/commonMain/kotlin/org/tiqian/layout/LineBreakPlanningStage.kt`
- `engine/src/commonMain/kotlin/org/tiqian/layout/LineBreaker.kt`
- `engine/src/commonMain/kotlin/org/tiqian/layout/LineRepair.kt`
- `engine/src/commonMain/kotlin/org/tiqian/layout/ParagraphDpLineBreaker.kt`
- `engine/src/commonMain/kotlin/org/tiqian/layout/ParagraphLayoutEngine.kt`
- `engine/src/commonMain/kotlin/org/tiqian/layout/ParagraphShapingStage.kt`
- `engine/src/commonMain/kotlin/org/tiqian/layout/PreparedParagraph.kt`
- `engine/src/commonMain/kotlin/org/tiqian/layout/ProgressiveBreakDecisions.kt`
- `engine/src/commonMain/kotlin/org/tiqian/layout/PunctuationGeometryLedger.kt`
- `engine/src/commonMain/kotlin/org/tiqian/layout/QuotePairAnalyzer.kt`
- `engine/src/commonMain/kotlin/org/tiqian/layout/UnicodePunctuationBoundaryResolver.kt`
- `engine/src/commonMain/kotlin/org/tiqian/layout/WidthIndependentAnnotationCache.kt`

来源用途：确认生产实现的行为、边界与性能意图。

### 来源 C：通用行为测试与测试基础设施（136 个文件）

#### CLREQ、core 与 font（29 个文件）

- `engine/src/commonTest/kotlin/org/tiqian/clreq/BopomofoParserTest.kt`
- `engine/src/commonTest/kotlin/org/tiqian/clreq/ClreqPolicyTailCoverageTest.kt`
- `engine/src/commonTest/kotlin/org/tiqian/clreq/ClreqProfileCoverageTest.kt`
- `engine/src/commonTest/kotlin/org/tiqian/clreq/ClreqPunctuationGlyphSubstitutorTest.kt`
- `engine/src/commonTest/kotlin/org/tiqian/clreq/KinsokuLevelTest.kt`
- `engine/src/commonTest/kotlin/org/tiqian/clreq/NumberSymbolCohesionTest.kt`
- `engine/src/commonTest/kotlin/org/tiqian/clreq/PunctuationGluePlacementTest.kt`
- `engine/src/commonTest/kotlin/org/tiqian/core/CoreBoundaryTest.kt`
- `engine/src/commonTest/kotlin/org/tiqian/core/CoreLayoutQueriesGapsTest.kt`
- `engine/src/commonTest/kotlin/org/tiqian/core/CoreUnitsGeometryTest.kt`
- `engine/src/commonTest/kotlin/org/tiqian/core/EastAsianSpacingCoverageTest.kt`
- `engine/src/commonTest/kotlin/org/tiqian/core/EastAsianSpacingLookupCoverageTest.kt`
- `engine/src/commonTest/kotlin/org/tiqian/core/EastAsianSpacingTest.kt`
- `engine/src/commonTest/kotlin/org/tiqian/core/LayoutQueriesResidualCoverageTest.kt`
- `engine/src/commonTest/kotlin/org/tiqian/core/LayoutQueriesTest.kt`
- `engine/src/commonTest/kotlin/org/tiqian/core/LinkAddressDisplayTest.kt`
- `engine/src/commonTest/kotlin/org/tiqian/core/SourceInteractionBoundariesCoverageTest.kt`
- `engine/src/commonTest/kotlin/org/tiqian/core/TextModelCoverageTest.kt`
- `engine/src/commonTest/kotlin/org/tiqian/core/TextRangeTest.kt`
- `engine/src/commonTest/kotlin/org/tiqian/core/UnicodeNumberTest.kt`
- `engine/src/commonTest/kotlin/org/tiqian/core/UnicodeScriptEvidenceTest.kt`
- `engine/src/commonTest/kotlin/org/tiqian/core/UnicodeWordCharacterTest.kt`
- `engine/src/commonTest/kotlin/org/tiqian/font/CjkDashCapabilityPolicyTest.kt`
- `engine/src/commonTest/kotlin/org/tiqian/font/CjkFontRoleClassifierTest.kt`
- `engine/src/commonTest/kotlin/org/tiqian/font/FontPolicyCoverageTest.kt`
- `engine/src/commonTest/kotlin/org/tiqian/font/FontRoleTailCoverageTest.kt`
- `engine/src/commonTest/kotlin/org/tiqian/font/InlineShapingStylePolicyTest.kt`
- `engine/src/commonTest/kotlin/org/tiqian/font/ScriptAwareFontMetricsNormalizerTest.kt`
- `engine/src/commonTest/kotlin/org/tiqian/font/UsesLatinFaceTest.kt`

#### layout（82 个文件）

- `engine/src/commonTest/kotlin/org/tiqian/layout/AnnotationGeometryStageCoverageTest.kt`
- `engine/src/commonTest/kotlin/org/tiqian/layout/AsciiPointMarkKinsokuTest.kt`
- `engine/src/commonTest/kotlin/org/tiqian/layout/AttachedInlineBoundaryRelocationTest.kt`
- `engine/src/commonTest/kotlin/org/tiqian/layout/AutoSpaceSingleGapTest.kt`
- `engine/src/commonTest/kotlin/org/tiqian/layout/BaselineAlignmentTest.kt`
- `engine/src/commonTest/kotlin/org/tiqian/layout/BilingualEmphasisTest.kt`
- `engine/src/commonTest/kotlin/org/tiqian/layout/BopomofoLayoutTest.kt`
- `engine/src/commonTest/kotlin/org/tiqian/layout/ClusterRoleResolutionCoverageTest.kt`
- `engine/src/commonTest/kotlin/org/tiqian/layout/ClusterRoleResolutionSurrogateAndExtenderEdgeTest.kt`
- `engine/src/commonTest/kotlin/org/tiqian/layout/ContextualDashEllipsisRoleResolverCoverageTest.kt`
- `engine/src/commonTest/kotlin/org/tiqian/layout/ContextualDashEllipsisRoleResolverTest.kt`
- `engine/src/commonTest/kotlin/org/tiqian/layout/ContextualQuoteRoleResolverCoverageTest.kt`
- `engine/src/commonTest/kotlin/org/tiqian/layout/ContextualQuoteRoleResolverNestedAndSurrogateTest.kt`
- `engine/src/commonTest/kotlin/org/tiqian/layout/DecideHyphenBreakTest.kt`
- `engine/src/commonTest/kotlin/org/tiqian/layout/DisplayGlyphSubstitutionEngineTest.kt`
- `engine/src/commonTest/kotlin/org/tiqian/layout/EmergencyGraphemeTrackingTest.kt`
- `engine/src/commonTest/kotlin/org/tiqian/layout/ExplainableStubParagraphLayoutEngineTest.kt`
- `engine/src/commonTest/kotlin/org/tiqian/layout/FontInstanceMetricsRequestTest.kt`
- `engine/src/commonTest/kotlin/org/tiqian/layout/GreedyLineBreakerTest.kt`
- `engine/src/commonTest/kotlin/org/tiqian/layout/HyphenationLayoutTest.kt`
- `engine/src/commonTest/kotlin/org/tiqian/layout/InlineBoxLayoutTest.kt`
- `engine/src/commonTest/kotlin/org/tiqian/layout/InlineObjectLayoutTest.kt`
- `engine/src/commonTest/kotlin/org/tiqian/layout/InterpunctShrinkOpportunityTest.kt`
- `engine/src/commonTest/kotlin/org/tiqian/layout/JustifierCompressionTest.kt`
- `engine/src/commonTest/kotlin/org/tiqian/layout/JustifierCoverageTest.kt`
- `engine/src/commonTest/kotlin/org/tiqian/layout/JustifierEngineTest.kt`
- `engine/src/commonTest/kotlin/org/tiqian/layout/JustifierJfTest.kt`
- `engine/src/commonTest/kotlin/org/tiqian/layout/JustifierTest.kt`
- `engine/src/commonTest/kotlin/org/tiqian/layout/KinsokuAndCohesionRepairEngineTest.kt`
- `engine/src/commonTest/kotlin/org/tiqian/layout/LayoutDumpFormat.kt`
- `engine/src/commonTest/kotlin/org/tiqian/layout/LayoutDumpGoldenParityTest.kt`
- `engine/src/commonTest/kotlin/org/tiqian/layout/LineAdjustmentPushInTest.kt`
- `engine/src/commonTest/kotlin/org/tiqian/layout/LineAdjustmentStageCoverageTest.kt`
- `engine/src/commonTest/kotlin/org/tiqian/layout/LineAdjustmentStageJfTest.kt`
- `engine/src/commonTest/kotlin/org/tiqian/layout/LineBreakPlanningStageCoverage2Test.kt`
- `engine/src/commonTest/kotlin/org/tiqian/layout/LineBreakPlanningStageCoverageTest.kt`
- `engine/src/commonTest/kotlin/org/tiqian/layout/LineBreakRepairEngineTest.kt`
- `engine/src/commonTest/kotlin/org/tiqian/layout/LineBreakerCoverage2Test.kt`
- `engine/src/commonTest/kotlin/org/tiqian/layout/LineCandidateValidationTest.kt`
- `engine/src/commonTest/kotlin/org/tiqian/layout/LineGeometryDirectTailTest.kt`
- `engine/src/commonTest/kotlin/org/tiqian/layout/LineOptimizationCoverageTest.kt`
- `engine/src/commonTest/kotlin/org/tiqian/layout/LineRepairCoverageTest.kt`
- `engine/src/commonTest/kotlin/org/tiqian/layout/LineRepairTailCoverageTest.kt`
- `engine/src/commonTest/kotlin/org/tiqian/layout/LookaheadLineBreakerTest.kt`
- `engine/src/commonTest/kotlin/org/tiqian/layout/OpeningBracketLineStartTest.kt`
- `engine/src/commonTest/kotlin/org/tiqian/layout/ParagraphDpLineBreakerCoverage2Test.kt`
- `engine/src/commonTest/kotlin/org/tiqian/layout/ParagraphDpLineBreakerCoverageTest.kt`
- `engine/src/commonTest/kotlin/org/tiqian/layout/ParagraphDpLineBreakerTest.kt`
- `engine/src/commonTest/kotlin/org/tiqian/layout/ParagraphDpTierPromotionPoolTest.kt`
- `engine/src/commonTest/kotlin/org/tiqian/layout/ParagraphLayoutEngineValidationCoverageTest.kt`
- `engine/src/commonTest/kotlin/org/tiqian/layout/ParagraphShapingStageCoverageTest.kt`
- `engine/src/commonTest/kotlin/org/tiqian/layout/PreparedParagraphInlineEdgesTest.kt`
- `engine/src/commonTest/kotlin/org/tiqian/layout/PreparedParagraphJfTest.kt`
- `engine/src/commonTest/kotlin/org/tiqian/layout/PreparedParagraphJsonNumberTest.kt`
- `engine/src/commonTest/kotlin/org/tiqian/layout/PreparedParagraphPlanConstructionTest.kt`
- `engine/src/commonTest/kotlin/org/tiqian/layout/ProgressiveBreakDecisionsCoverageTest.kt`
- `engine/src/commonTest/kotlin/org/tiqian/layout/ProgressiveBreakDecisionsTailTest.kt`
- `engine/src/commonTest/kotlin/org/tiqian/layout/ProgressiveTechnicalBreakTest.kt`
- `engine/src/commonTest/kotlin/org/tiqian/layout/PunctuationAtomBuilderHaltTest.kt`
- `engine/src/commonTest/kotlin/org/tiqian/layout/PunctuationBodyFloorInvariantTest.kt`
- `engine/src/commonTest/kotlin/org/tiqian/layout/PunctuationGeometryBranchArmsCoverageTest.kt`
- `engine/src/commonTest/kotlin/org/tiqian/layout/PunctuationGeometryEngineTest.kt`
- `engine/src/commonTest/kotlin/org/tiqian/layout/PunctuationGeometryLedgerCoverageTest.kt`
- `engine/src/commonTest/kotlin/org/tiqian/layout/PunctuationGeometryStageCoverageTest.kt`
- `engine/src/commonTest/kotlin/org/tiqian/layout/PunctuationModelCoverageTest.kt`
- `engine/src/commonTest/kotlin/org/tiqian/layout/PunctuationSpacingRuleTest.kt`
- `engine/src/commonTest/kotlin/org/tiqian/layout/PushInLineWideCapacityTest.kt`
- `engine/src/commonTest/kotlin/org/tiqian/layout/QuoteClassificationEngineTest.kt`
- `engine/src/commonTest/kotlin/org/tiqian/layout/QuotePairAnalyzerCoverageTest.kt`
- `engine/src/commonTest/kotlin/org/tiqian/layout/QuotePairAnalyzerSurrogateAdjacencyTest.kt`
- `engine/src/commonTest/kotlin/org/tiqian/layout/QuotePairAnalyzerTest.kt`
- `engine/src/commonTest/kotlin/org/tiqian/layout/R3GeometryTailCoverageTest.kt`
- `engine/src/commonTest/kotlin/org/tiqian/layout/RecordedEvidenceGoldenParityTest.kt`
- `engine/src/commonTest/kotlin/org/tiqian/layout/RubyLayoutTest.kt`
- `engine/src/commonTest/kotlin/org/tiqian/layout/SpacingAndLineGeometryEngineTest.kt`
- `engine/src/commonTest/kotlin/org/tiqian/layout/UnicodeEmoji17RgiRoleAuditTest.kt`
- `engine/src/commonTest/kotlin/org/tiqian/layout/UnicodePunctuationBoundaryResolverCoverageTest.kt`
- `engine/src/commonTest/kotlin/org/tiqian/layout/UnicodePunctuationBoundaryTest.kt`
- `engine/src/commonTest/kotlin/org/tiqian/layout/VerbatimRangeAutoSpaceTest.kt`
- `engine/src/commonTest/kotlin/org/tiqian/layout/WidthIndependentAnnotationCacheCoverageTest.kt`
- `engine/src/commonTest/kotlin/org/tiqian/layout/WidthIndependentAnnotationCacheTest.kt`
- `engine/src/commonTest/kotlin/org/tiqian/layout/ZeroWidthBreakControlLayoutTest.kt`

#### linebreak、shaping 与测试基础设施（25 个文件）

- `engine/src/commonTest/kotlin/org/tiqian/linebreak/EnglishHyphenationTest.kt`
- `engine/src/commonTest/kotlin/org/tiqian/linebreak/LiangHyphenatorTest.kt`
- `engine/src/commonTest/kotlin/org/tiqian/linebreak/LineBreakCoverageTest.kt`
- `engine/src/commonTest/kotlin/org/tiqian/linebreak/MandatoryBreakTest.kt`
- `engine/src/commonTest/kotlin/org/tiqian/linebreak/UnicodePunctuationLineBreakCoverageTest.kt`
- `engine/src/commonTest/kotlin/org/tiqian/linebreak/UnicodePunctuationLineBreakTest.kt`
- `engine/src/commonTest/kotlin/org/tiqian/shaping/ExplainableStubTextShaperTest.kt`
- `engine/src/commonTest/kotlin/org/tiqian/shaping/ReplayableFontBackendCoverageTest.kt`
- `engine/src/commonTest/kotlin/org/tiqian/shaping/TextShaperCoverageTest.kt`
- `engine/src/commonTest/kotlin/org/tiqian/test/LayoutFixtures.kt`
- `engine/src/commonTest/kotlin/org/tiqian/test/ShapingEvidence.kt`
- `engine/src/commonTest/kotlin/org/tiqian/test/ShapingEvidenceJson.kt`
- `engine/src/commonTest/kotlin/org/tiqian/test/trace/TestTracePlatform.kt`
- `engine/src/commonTest/kotlin/org/tiqian/test/trace/TestTraceRender.kt`
- `engine/src/commonTest/kotlin/org/tiqian/test/trace/TestTraceStore.kt`
- `engine/src/commonTest/kotlin/org/tiqian/test/trace/TraceFormat.kt`
- `engine/src/commonTest/kotlin/org/tiqian/test/trace/TracedAssertions.kt`
- `engine/src/commonTest/kotlin/org/tiqian/trace/TraceScenario.kt`
- `engine/src/commonTest/kotlin/org/tiqian/trace/TraceScenarios.kt`
- `engine/src/commonTest/kotlin/org/tiqian/trace/TraceScenariosClreq.kt`
- `engine/src/commonTest/kotlin/org/tiqian/trace/TraceScenariosCore.kt`
- `engine/src/commonTest/kotlin/org/tiqian/trace/TraceScenariosFont.kt`
- `engine/src/commonTest/kotlin/org/tiqian/trace/TraceScenariosLayout.kt`
- `engine/src/commonTest/kotlin/org/tiqian/trace/TraceScenariosLinebreak.kt`
- `engine/src/commonTest/kotlin/org/tiqian/trace/TraceScenariosShaping.kt`

来源用途：提供跨平台行为断言和测试基础设施；测试映射按 T1–T5 工作项进行，不按目录状态追踪。

### 来源 D：JVM golden、layout 工具与记录证据（68 个文件）

#### Kotlin 测试与工具（9 个文件）

- `engine/src/jvmTest/kotlin/org/tiqian/layout/GiantTokenScalingProbe.kt`
- `engine/src/jvmTest/kotlin/org/tiqian/layout/LayoutDumpGoldenTest.kt`
- `engine/src/jvmTest/kotlin/org/tiqian/layout/ParagraphDpReferenceExperiment.kt`
- `engine/src/jvmTest/kotlin/org/tiqian/layout/ParagraphDpTuningProbe.kt`
- `engine/src/jvmTest/kotlin/org/tiqian/layout/PreparedParagraphRenderEvidenceTest.kt`
- `engine/src/jvmTest/kotlin/org/tiqian/layout/ShapingEvidenceRecorderTest.kt`
- `engine/src/jvmTest/kotlin/org/tiqian/layout/tooling/LayoutReportMain.kt`
- `engine/src/jvmTest/kotlin/org/tiqian/test/trace/TestTracePlatform.jvm.kt`
- `engine/src/jvmTest/kotlin/org/tiqian/trace/TraceGoldenTest.kt`

#### Golden 与 shaping resources（59 个文件）

- `engine/src/jvmTest/resources/golden/layout-dumps-recorded/adaptive-short-line-indent.txt`
- `engine/src/jvmTest/resources/golden/layout-dumps-recorded/adjacent-curly-quote-list-context.txt`
- `engine/src/jvmTest/resources/golden/layout-dumps-recorded/adjacent-punctuation-spacing.txt`
- `engine/src/jvmTest/resources/golden/layout-dumps-recorded/ascii-brackets-in-cjk.txt`
- `engine/src/jvmTest/resources/golden/layout-dumps-recorded/ascii-point-mark-impossible-measure.txt`
- `engine/src/jvmTest/resources/golden/layout-dumps-recorded/ascii-point-mark-in-cjk.txt`
- `engine/src/jvmTest/resources/golden/layout-dumps-recorded/basic-pause-stop.txt`
- `engine/src/jvmTest/resources/golden/layout-dumps-recorded/bibliographic-numeric-locator-break.txt`
- `engine/src/jvmTest/resources/golden/layout-dumps-recorded/bopomofo-tone-em-box.txt`
- `engine/src/jvmTest/resources/golden/layout-dumps-recorded/contextual-curly-quotes.txt`
- `engine/src/jvmTest/resources/golden/layout-dumps-recorded/contextual-dash-ellipsis.txt`
- `engine/src/jvmTest/resources/golden/layout-dumps-recorded/ellipsis-and-dash.txt`
- `engine/src/jvmTest/resources/golden/layout-dumps-recorded/emphasis-marks.txt`
- `engine/src/jvmTest/resources/golden/layout-dumps-recorded/fallback-roles.txt`
- `engine/src/jvmTest/resources/golden/layout-dumps-recorded/first-line-indent.txt`
- `engine/src/jvmTest/resources/golden/layout-dumps-recorded/greedy-multi-line.txt`
- `engine/src/jvmTest/resources/golden/layout-dumps-recorded/indent-opening-quote.txt`
- `engine/src/jvmTest/resources/golden/layout-dumps-recorded/interlinear-lines.txt`
- `engine/src/jvmTest/resources/golden/layout-dumps-recorded/justify-cjk-paragraph.txt`
- `engine/src/jvmTest/resources/golden/layout-dumps-recorded/justify-mixed-paragraph.txt`
- `engine/src/jvmTest/resources/golden/layout-dumps-recorded/justify-unbreakable-number-symbol.txt`
- `engine/src/jvmTest/resources/golden/layout-dumps-recorded/kinsoku-carry-previous.txt`
- `engine/src/jvmTest/resources/golden/layout-dumps-recorded/kinsoku-push-in.txt`
- `engine/src/jvmTest/resources/golden/layout-dumps-recorded/latin-camelcase.txt`
- `engine/src/jvmTest/resources/golden/layout-dumps-recorded/latin-existing-hyphen.txt`
- `engine/src/jvmTest/resources/golden/layout-dumps-recorded/latin-hard-break.txt`
- `engine/src/jvmTest/resources/golden/layout-dumps-recorded/latin-opaque-url-token.txt`
- `engine/src/jvmTest/resources/golden/layout-dumps-recorded/latin-word-wrap.txt`
- `engine/src/jvmTest/resources/golden/layout-dumps-recorded/line-end-kinsoku.txt`
- `engine/src/jvmTest/resources/golden/layout-dumps-recorded/lookahead-avoids-repair.txt`
- `engine/src/jvmTest/resources/golden/layout-dumps-recorded/lookahead-future-push-in.txt`
- `engine/src/jvmTest/resources/golden/layout-dumps-recorded/mandatory-blank-lines.txt`
- `engine/src/jvmTest/resources/golden/layout-dumps-recorded/mandatory-crlf.txt`
- `engine/src/jvmTest/resources/golden/layout-dumps-recorded/mandatory-leading-trailing-newline.txt`
- `engine/src/jvmTest/resources/golden/layout-dumps-recorded/mandatory-single-newline.txt`
- `engine/src/jvmTest/resources/golden/layout-dumps-recorded/mandatory-wraps-long-line.txt`
- `engine/src/jvmTest/resources/golden/layout-dumps-recorded/mi10s-adjacent-curly-quote-wrap.txt`
- `engine/src/jvmTest/resources/golden/layout-dumps-recorded/mi10s-western-bracket-citation-wrap.txt`
- `engine/src/jvmTest/resources/golden/layout-dumps-recorded/mixed-script-quote-paragraph-language.txt`
- `engine/src/jvmTest/resources/golden/layout-dumps-recorded/mourning-frame.txt`
- `engine/src/jvmTest/resources/golden/layout-dumps-recorded/nested-quotes.txt`
- `engine/src/jvmTest/resources/golden/layout-dumps-recorded/parenthetical-dash-pairs.txt`
- `engine/src/jvmTest/resources/golden/layout-dumps-recorded/progressive-technical-alpha-numeric.txt`
- `engine/src/jvmTest/resources/golden/layout-dumps-recorded/progressive-technical-current-line-emergency.txt`
- `engine/src/jvmTest/resources/golden/layout-dumps-recorded/progressive-technical-hash-fill.txt`
- `engine/src/jvmTest/resources/golden/layout-dumps-recorded/progressive-technical-inline.txt`
- `engine/src/jvmTest/resources/golden/layout-dumps-recorded/quote-digit-boundaries.txt`
- `engine/src/jvmTest/resources/golden/layout-dumps-recorded/real-paragraph-1.txt`
- `engine/src/jvmTest/resources/golden/layout-dumps-recorded/ruby-line-height.txt`
- `engine/src/jvmTest/resources/golden/layout-dumps-recorded/unmatched-curly-quotes.txt`
- `engine/src/jvmTest/resources/golden/layout-dumps-recorded/western-hyphenation.txt`
- `engine/src/jvmTest/resources/golden/layout-dumps-recorded/zero-width-space-soft-break.txt`
- `engine/src/jvmTest/resources/golden/layout-dumps/contextual-dash-ellipsis.txt`
- `engine/src/jvmTest/resources/golden/layout-dumps/ellipsis-and-dash.txt`
- `engine/src/jvmTest/resources/golden/layout-dumps/fallback-roles.txt`
- `engine/src/jvmTest/resources/golden/layout-dumps/parenthetical-dash-pairs.txt`
- `engine/src/jvmTest/resources/golden/layout-dumps/quote-digit-boundaries.txt`
- `engine/src/jvmTest/resources/golden/layout-dumps/real-paragraph-1.txt`
- `engine/src/jvmTest/resources/golden/shaping-evidence.json`

来源用途：提供 fixture、golden、layout 工具和 recorded shaping evidence 的可观察基线。

### 测试资产重新核验与实施前分组（2026-09-01）

为防止旧目录清单在后续逐文件审计中遗漏或漂移，本轮开始前重新从本文件开头记录的 Git 区间执行
`tools/list_kotlin_test_audit.py`。该脚本仅调用相邻 Kotlin checkout 的 Git 元数据，不写 Kotlin 或 Rust
工作区，也不依赖 shell pipeline。它按 NUL 分隔的 `git diff --name-status` 读取路径；重命名条目以目标路径
计入，未归类路径会以非零退出阻止审计继续。

重新生成结果与上方清单一致：`engine/src` 共 237 个变更路径，其中测试资产 208 个；包含 149 个 Kotlin
源码与 59 个 JVM golden/evidence 资源。source set 计数为 commonTest 136、jvmTest 68、androidHostTest 1、
jsTest 1、linuxX64Test 1、nativeTest 1。所有 208 个路径均已被唯一归入下列职责工作组；脚本当前没有
`unclassified` 输出。

| 测试映射工作项 | 资产数 | 范围与检查重点 |
| --- | ---: | --- |
| fixture 与记录 evidence | 92 | `LayoutFixtures`、shaping evidence、trace、golden、layout report、prepared/render evidence；核对跨工作区 fixture 协议、可解释 dump 与平台专属 trace 边界。 |
| 基础 CLREQ、core、font | 35 | Unicode、East Asian spacing、文本模型、linebreak、font policy 与 metrics；逐函数区分 Rust 等价实现、需补测试和 Kotlin 宿主规则。 |
| 标点几何与行内布局 | 24 | punctuation ink/HALT/body/glue、autospace、kinsoku、inline object/box、ruby/Bopomofo、行内几何。 |
| shaping 与 Unicode 边界 | 22 | role resolution、quote/dash、display substitution、cluster/cache、emoji 与 Unicode boundary；已完成的 P0 行为仍需记录对应测试函数。 |
| 断行与行调整 | 35 | greedy/lookahead/DP、repair、push-in、justify、hyphenation、progressive break、zero-width break 与验证候选。 |

检查顺序按上表执行。每个工作项先用脚本的 `--paths` 输出锁定输入文件，再分小组读取 Kotlin 测试和相关 Rust 测试，
在本文件记录“测试函数 → Rust 对应物 → 结论 → 验证”。只有五批全部完成、未处理行为均有明确处置后，才开始新的同步实现。

### 来源 E：平台、native 与 ABI（9 个文件）

- `engine/src/androidHostTest/kotlin/org/tiqian/test/trace/TestTracePlatform.android.kt`
- `engine/src/jsMain/kotlin/org/tiqian/shaping/HarfBuzzSessionBackend.kt`（删除）
- `engine/src/jsTest/kotlin/org/tiqian/test/trace/TestTracePlatform.js.kt`
- `engine/src/linuxX64Test/kotlin/org/tiqian/shaping/NativeFontBackendTest.kt`
- `engine/src/nativeInterop/cinterop/tiqian_font_backend.h`
- `engine/src/nativeMain/kotlin/org/tiqian/layout/PlanPackedWriter.kt`
- `engine/src/nativeMain/kotlin/org/tiqian/shaping/NativeFontBackendShaper.kt`
- `engine/src/nativeMain/kotlin/org/tiqian/shaping/NativeFontBackendVtable.kt`
- `engine/src/nativeTest/kotlin/org/tiqian/test/trace/TestTracePlatform.native.kt`

来源用途：划分 Kotlin 平台、Native ABI 与独立 Rust 排版核心的边界。

## 统一工作项状态

本表是本轮同步的唯一状态来源。状态只能为“未开始”“进行中”“完成”“不适用”或“阻塞”；来源 A–E
和后文调查记录均不单独表达进度。每个工作项完成前，必须在对应映射记录中列出 Kotlin 输入、Rust 对应物、
结论和实际验证证据。

| 工作项 | 状态 | 范围 | 映射记录 |
| --- | --- | --- | --- |
| P0-1 | 完成 | contextual dash/ellipsis、parenthetical pair、role-gated display substitution 与 fixture | “实施工作项与验证” |
| P0-2 | 完成 | 数字引号、apostrophe 边界、numeric prime 与 fixture | “实施工作项与验证” |
| P0-3 | 完成 | prepared JSON number 与 render evidence | “实施工作项与验证” |
| P1 | 完成 | `PunctuationClass::Quote` 清理 | “实施工作项与验证” |
| P2 | 完成 | recorded shaping evidence replay 与跨语言 dump parity | “实施工作项与验证” |
| P3 | 完成 | `UnbreakableRanges`、interval overlap、cluster range 查询性能索引与 80k giant-token 热路径优化 | “实施工作项与验证” |
| T1 | 完成 | fixture、golden、recorded evidence、trace、layout report 与 render evidence | “测试映射工作项” |
| T2 | 完成 | CLREQ、core、font、Unicode 与 linebreak 基础行为 | “测试映射工作项” |
| T3 | 完成 | 标点 geometry、autospace、kinsoku、inline layout、ruby 与 Bopomofo | “测试映射工作项” |
| T4 | 完成 | shaping、role/cache、quote/dash、Unicode/emoji 边界 | “测试映射工作项” |
| T5 | 完成 | line breaker、repair、push-in、justify、hyphenation 与 progressive break | “测试映射工作项” |

## 工作规则

- 每次只选择一个 P/T 工作项中与当前主题相关的少量文件；先读取提交主题和 diff 概览，再按需读取实现与测试。
- 每个小组结束后，立即在本文件写明 Kotlin 测试函数、Rust 对应物、结论、验证证据和下一步；不以对话上下文保存结论。
- 每个 T 先完成全部函数或资产级映射并记录迁移范围；随后只迁移该 T 的明确项目、运行相关验证并回写证据，完成后再开始下一个 T。
- 审计完成前不更新 `docs/tracking.md` 的“当前状态”或日志摘要；该页只在本轮同步实际完成后更新。
- 本文件的 277 路径清单完整，但不表示每个测试函数均已完成 Kotlin→Rust 映射；T1–T5 全部完成才代表测试映射完成。

文件清单校验：归档中的 277 个 Kotlin `docs/`、`engine/src/` 路径与区间 `git diff --name-only`
逐项一致，无遗漏；额外出现的 `docs/tracking.md` 是本仓库流程说明，不属于 Kotlin diff。

## 生产引擎调查

本节只处理上游 `engine/src/commonMain` 与 `engine/src/nativeMain` 的生产源码：共 27 个文件，
其中 24 个是跨平台核心，3 个是 Kotlin/Native 接口。区间内没有 `jvmMain`、`androidMain`、
`appleMain` 的生产源码变更。每个文件均已按 Kotlin diff、提交意图和 Rust 对应 pipeline 核对；测试文件
只在本节用于确认变更意图，尚未进行测试映射。

状态含义：**必须同步**表示 Rust 当前引擎的可观察布局、display、结构化 debug 或已公开输出与 Kotlin 有差异；
**性能等价**表示 Kotlin 明确保持既有结果与优先级；**不适用**表示变更属于 Kotlin 宿主、Native ABI 或
Kotlin 私有 API。下表覆盖全部生产文件，不以测试文件名称推断结论。

| Kotlin 生产文件 | 上游变更 | Rust 对应物 | 结论 |
| --- | --- | --- | --- |
| `clreq/ClreqProfile.kt` | 删除永不产生的 `PunctuationClass.Quote`，将 substitutor 改为 internal 可见性 | `clreq/clreq_profile.rs` 的公开 `PunctuationClass::Quote` 与两处不可达分支 | 归入 P1，不影响当前布局；公开 enum 变更须单列决定。 |
| `core/UnicodeNumberData.kt`、`core/UnicodeWordCharacter.kt` | 固定 Unicode 17 的 Nd/Nl/No 判断，供数字引号规则使用 | `core/unicode_word_character.rs` 以 ICU4X Unicode 17 判断 `is_number` | 能力已具备；数字引号同步可直接复用，不移植生成范围表。 |
| `core/UnicodeScriptEvidence.kt` | 将文档中的 quote 描述扩展到 contextual marks | `core/unicode_script_evidence.rs` | 已等价，无行为改动。 |
| `font/CjkDashCapabilityPolicy.kt` | Kotlin host 的 CJK dash probe capability 名称 | 无 Rust Web/Native host probe | 不适用。 |
| `font/FontPolicy.kt` | 明确 U+2014/U+2026 基础分类后由段落 resolver 覆盖 | `font/font_policy.rs` | 必须同步项的下游依赖；基础 CJK 默认已存在，缺 paragraph override。 |
| `font/InlineShapingStylePolicy.kt` | DOM inherited CSS property 降级规则 | 无 DOM/CSS lowering | 不适用。 |
| `layout/ClusterRoleResolution.kt` | Latin-resolved U+2014/U+2026 run 保持独立 cluster，重复合并仍受 profile 限制 | `layout/cluster_role_resolution.rs` | 必须同步，不能只新增 classifier；否则断行边界不同。 |
| `layout/ContextualDashEllipsisRoleResolver.kt` | 周边强脚本、强制换行边界、段落语言回退和 parenthetical dash pair | 无对应模块 | 必须同步。 |
| `layout/ContextualPunctuationDisplaySubstitution.kt` | 仅 CJK role 可作 CLREQ display-codepoint substitution | `layout/paragraph_shaping_stage.rs` 直接调用 `substitute` | 必须同步，Latin dash/ellipsis 必须保留 source code point。 |
| `layout/ContextualQuoteRoleResolver.kt` | non-CJK word pair、数字 prime 的未配对 closing quote | `layout/contextual_quote_role_resolver.rs` | 必须同步：缺 `NumericPrimeUnmatchedQuote`。成对引号主体已存在。 |
| `layout/LayoutDebugAssembly.kt` | debug display/reason 走 role-gated substitution | `layout/layout_debug_assembly.rs` | 随 dash/ellipsis 同步；否则结构化 debug 与绘制 display 不同源。 |
| `layout/QuotePairAnalyzer.kt` | 数字/全角外侧限制、apostrophe 至少一侧非数字非全角、数字 prime helper | `layout/quote_pair_analyzer.rs` | 必须同步：paired quote 外侧限制已等价；apostrophe 限制和 prime helper 缺失。 |
| `layout/UnicodePunctuationBoundaryResolver.kt` | valid UTF-16 前提下的 BMP 固定步长及 IS mark 读取修正 | `layout/unicode_punctuation_boundary_resolver.rs` | 有效 UTF-8 输入上已等价；不移植 Kotlin surrogate 防御删减。 |
| `layout/WidthIndependentAnnotationCache.kt` | quote-aware classifier 外再包 dash-aware classifier，并记录 role override | `layout/width_independent_annotation_cache.rs` | 必须同步，是 dash/ellipsis 接入实际 pipeline 的装配点。 |
| `layout/ParagraphShapingStage.kt` | shaping 与 rollback 使用 role-gated display substitution | `layout/paragraph_shaping_stage.rs` | 必须同步，和 display substitution 作为同一提交单元。 |
| `layout/ParagraphLayoutEngine.kt` | 中间名称改动后恢复 `ExplainableStubParagraphLayoutEngine` | `layout/paragraph_layout_engine.rs` 已使用该名称 | 已等价，无行为改动。 |
| `layout/LineBreakPlanningStage.kt` | 不可断区间、technical overlap 和 emergency decision 查询索引化 | `layout/line_break_planning_stage.rs` | 性能等价；结果和 decision-list 优先级保持不变。 |
| `layout/LineBreaker.kt`、`layout/LineRepair.kt`、`layout/ParagraphDpLineBreaker.kt` | 调用方改为使用 `UnbreakableRanges` 索引 | `layout/line_breaker.rs`、`layout/line_repair.rs`、`layout/paragraph_dp_line_breaker.rs` | 性能等价；与前项合并，先用 Rust 病理输入确认热点再移植。 |
| `layout/ProgressiveBreakDecisions.kt` | `UnbreakableRanges` 的 sorted-start / prefix-max-last 查询 | `layout/progressive_break_decisions.rs` 仍线性扫描 | 归入 P3；固定点退回与原 list 顺序不变。 |
| `layout/PunctuationGeometryLedger.kt` | source 有序、不重叠前提下的 cluster range 二分查找 | `layout/punctuation_geometry_ledger.rs` 仍线性扫描 | 归入 P3；先验证 Rust cluster 不变量和基准。 |
| `layout/PreparedParagraph.kt` | exact float round-trip JSON number；可选 `renderEvidence` 字段、ruby ascent | `layout/prepared_paragraph.rs` 已公开同名基础 JSON API | 必须同步公开 JSON 兼容要求。默认 JSON 的数字格式必须字节一致；扩展 API 应接收 `renderEvidence` 并按 Kotlin 字段规则输出。 |
| `nativeMain/layout/PlanPackedWriter.kt` | Kotlin Native packed plan writer | Rust 无 packed plan ABI producer | 不适用。 |
| `nativeMain/shaping/NativeFontBackendShaper.kt` | Kotlin/Native font-family pointer-array ABI | Rust 无 Kotlin Native host callback ABI | 不适用。 |
| `nativeMain/shaping/NativeFontBackendVtable.kt` | Kotlin Native font backend protocol revision 2 | Rust 无该 vtable | 不适用。 |

生产引擎调查结论：27 个生产文件的事实和 Rust 映射证据已经记录；实施范围、顺序和验证方法
只以本文件“实施工作项与验证”为准。测试覆盖仍未完成 Kotlin→Rust 映射，故该清单的测试部分会随审计补充，
不能从本节推断测试工作已经结束。

## 历史调查记录

本节按实际调查主题保留结论形成过程。标题中的来源编号仅用于定位输入，不表示当前进度；当前状态以
“统一工作项状态”表为准。

### 来源 A：决策、架构与研究文档（相关规则筛查）

已读取 `0050`、`0054`、`0056`、`0057` 与区间内 docs/ADR 提交主题。当前事实如下：

| 上游主题 | ADR 状态 | 对 `tiqian-rs` 的初步影响 | 后续检查 |
| --- | --- | --- | --- |
| 原生 FFI、打包 plan、字体回调 vtable 与 Rust precompute | ADR 0050 已接受，且有多次修订 | `tiqian-rs` 是独立 Rust 排版实现；不应把 Kotlin/Native ABI 或 Kotlin 上游 precompute crate 当作本仓库的核心同步目标。需单独检查是否有可共享的 plan/fixture 协议变化。 | 来源 D、E 和 T1 核对 fixture、evidence 与 ABI 边界。 |
| engine 合并为单模块、测试基础设施迁入 engine | ADR 0056 已接受 | Kotlin 的目录变动本身不改变 Rust 模块边界；`LayoutFixtures`、shaping evidence 与 trace 迁入会影响上游测试和 fixture 的发现路径。 | 来源 C、D 和 T1 核对测试协议与 Rust 工具。 |
| 无条件格量化、Double 格数算术、区间 plan 表 | ADR 0054 为 Proposed；执行清单仍未勾选 | 不能作为已发生的 Kotlin 行为同步，也不应据此修改 Rust 布局或缓存。 | 仅在当前代码和测试出现已实施部分时记录事实；来源 B、C、D 与 P3 核对。 |
| 纵轴退出缓存与装饰 em 化 | ADR 0057 为 Proposed | 当前不作为同步项。 | 仅在实现或 fixture 显示已提前实现时记录。 |
| Web prose host 收敛 | ADR 0053 为 Proposed | 属 Kotlin Web 宿主、FFI 和 precompute 分层；Rust 排版核心不直接复制。 | 来源 D、E 与 T1 仅核对 fixture、ABI 或可解释输出协议。 |

区间内与当前布局核心直接相关、但尚未细读的文档提交主题包括：contextual dash/ellipsis 与
parenthetical dash pair 的决策、引号内部边界规则、测试基础设施迁入 engine，以及记录 evidence
和 fixture 的路径重组。这些主题分别由来源 B/C 的调查记录和 T1、T4 处理。

已进一步读取 ADR 0003 的 `ContextualDashEllipsisRoleResolution` amendment、ADR 0040 的非 CJK
词内引号/prime amendment、`clreq-punctuation-audit.md` 对字体面与断行分层的说明，以及 ADR 0050 的
plan number canonicalization 段。它们分别确认：

- dash/ellipsis 的 surrounding-script、mandatory-break、standalone segmentation、parenthetical pair 和
	`CjkRoleGatedDisplaySubstitution` 是已记录的 engine 规则，和来源 B/C 的代码结论一致；
- apostrophe 的“至少一侧非数字、非全角”限制与 `NumericPrimeUnmatchedQuote` 是已记录的 engine 规则，
	和来源 B/C 的 quote 差异一致；
- `UnicodeNumber`、`UnicodeWordCharacter`、`Scripts` 与 line-break 数据必须固定 Unicode 17，Rust 已用
	ICU4X Unicode 17 覆盖 number helper；
- ADR 0050 的 plan JSON / packed plan parity 服务 Kotlin Native 与其 precompute binding，不能据此把
	Kotlin C ABI 引入独立 Rust layout crate。

调查结论：其余命中均为 Web host、Compose/Android、研究记录或 Proposed ADR，不新增本仓库
核心实施项。

### 来源 B / C：contextual dash、ellipsis 与引号角色

已读取 Kotlin `ContextualDashEllipsisRoleResolver.kt`、`ContextualQuoteRoleResolver.kt`、
`ContextualDashEllipsisRoleResolverTest.kt`，以及 Rust 的 quote resolver、cluster role stage
和现有 quote 测试。

| 上游行为 | Rust 对应物 | 当前结论 | 后续 |
| --- | --- | --- | --- |
| U+2014 / U+2026 按相邻强脚本角色解析；冲突或无证据时按段落语言回退；强制换行截断上下文 | 无独立 dash/ellipsis resolver；`cluster_role_resolution.rs` 仍依赖基础 classifier | 待同步。Kotlin `079628ad` 引入此行为，上一轮 fixture parity 的 `ellipsis-and-dash` 差异由其 role override 造成。 | 对照 Kotlin stage 装配、Rust font policy 与 fixture/dump，确定最小移植范围和测试矩阵。 |
| 相同长度的纯 em-dash run 可跨 word/ASCII 空格配成 parenthetical pair，使用外侧脚本联合解析；ellipsis、标点、符号、不等长度或换行均不配对 | 无对应实现 | 待同步，属于 `63cf3de1` 修复的组成部分。 | 归入 P0-1。 |
| paired / unmatched curly quote 的外层、内容、段落语言与空白分隔 Western run 决策 | `src/layout/contextual_quote_role_resolver.rs` | 已同步主体。Rust 代码的解析顺序、嵌套 pair 跳过、UTF-16 scalar 处理和结构化 source/reason 与当前 Kotlin 基本逐段对应。 | 只核对本区间新增的 prime、apostrophe、数字与全角边界规则。 |
| unmatched closing curly quote 紧邻数字作为 prime，不作 quotation 处理 | Kotlin `NumericPrimeUnmatchedQuote` 新增分支；当前 Rust `resolve_unmatched` 在 apostrophe 后直接查邻近脚本 | 待同步，尚未发现 Rust 同名分支。 | 阅读 Rust quote helper 与 Kotlin 边界测试，确认 analyzer 是否已经在更早阶段吸收该用例，并补充精确对照表。 |
| non-CJK word-internal quote、全角和数字边界、in-word apostrophe | Rust 现有 `quote_pair_analyzer.rs` 与 quote 测试已有多项同名覆盖 | 需要逐条核对，暂不下结论。 | 下一小批读取对应 helper 与 Kotlin 增量测试。 |

本小批的上游提交序列：`079628ad`（contextual dash/ellipsis）、`68c8836f`（Western run
cluster）、`63cf3de1`（parenthetical dash pair）；引号边界为 `3f0728ed`、`b7b7b76a`、
`e050c578`、`e1e06f6c`、`36f9d628` 与 `7b5142ff`。本轮尚未修改 Rust 代码。

#### 引号边界逐项结论

| Kotlin 规则 | Rust 状态 | 证据与处理 |
| --- | --- | --- |
| 非 CJK word-internal 成对引号走 Latin；内部只允许 non-CJK word character | 已同步 | Rust `is_non_cjk_word_internal_quote_pair` 同样逐 code point 检查内容；`quote_classification_engine_test.rs` 覆盖普通、空内容、supplementary letter 与数字内容。 |
| 成对引号外侧必须非数字且非全角，纯数字边界保持可配对 | 部分差异 | Rust 外侧 helper 已排除数字和 East Asian Width Fullwidth，与 Kotlin 当前实现一致；但 apostrophe helper 仍把数字-数字视为 in-word apostrophe，因而会阻止 `1‘2’3` 的 single pair。Kotlin `e1e06f6c` 要求两个 flank 均为 word character，且至少一个为非数字、非全角。待同步。 |
| `don’t`、`90’s` 等非 CJK in-word apostrophe 保持 Latin | 已同步主体 | Rust 现有 helper 与测试覆盖 Latin、Greek、Cyrillic、combining mark 和 supplementary 文本；补前项时需保持这些样例。 |
| 数字后未配对 U+2019 / U+201D 作为 prime | 待同步 | Kotlin `36f9d628` 的 `NumericPrimeUnmatchedQuote` 在 quote resolver 的 apostrophe 分支之后、邻近脚本搜索之前执行；Rust 无 `is_digit_bound_closing_quote`。应新增 `1’30”`、`6.1”` 及成对重音引号的回归。 |
| Kotlin lone-surrogate walk arm 覆盖 | 不适用 | Kotlin 测试用运行时 char code 保留非法 UTF-16；Rust `Text` 只接受有效 UTF-8，不能构造同一输入。有效 supplementary scalar 的 UTF-16 行走已由 Rust 现有测试覆盖。 |

调查结论：dash/ellipsis resolver、parenthetical dash pair、apostrophe 数字限制和 numeric prime
均已归入 P0-1 或 P0-2；quote pair 的外侧非数字/非全角判定与大部分结构化 resolver 已同步。来源 B/C
的此主题已完成生产代码对照；stage 装配已在本文件“生产引擎审计”逐项核实。

#### Pipeline 装配核对

Kotlin `WidthIndependentAnnotationCache.kt` 的执行顺序是：先解析 quote pair 并以
`QuotePairAwareFontRoleClassifier` 覆盖基础 classifier，再解析 dash/ellipsis，并由
`ContextualDashEllipsisAwareFontRoleClassifier` 包住 quote-aware classifier；两类 override info
共同进入结构化 debug 输出。`ClusterRoleResolution.kt` 还将 Latin-resolved dash/ellipsis run 保持为
独立 cluster，重复标记仅在 profile coalesce set 允许时合并，避免被相邻 Latin token 吞入。

Rust `width_independent_annotation_cache.rs` 目前只构造 `QuotePairAwareFontRoleClassifier`；
`cluster_role_resolution.rs` 没有 dash/ellipsis 独立 cluster 分支。因此移植 dash/ellipsis 时必须
同时补 resolver、cache 装配、role override debug info 和 cluster segmentation；只新增 classifier
规则不足以取得 Kotlin 的断行边界和 dump 行为。

### 来源 C：CLREQ、Unicode 与字体策略

已读取本批 7 个 Kotlin commonMain 文件的 diff，并核对 Rust number、CLREQ 和 font policy 的
对应实现。

| Kotlin 变更 | Rust 对应物 | 结论 |
| --- | --- | --- |
| `UnicodeNumber`：Unicode 17 Nd/Nl/No 成员测试，供 numeric prime 使用 | `src/core/unicode_word_character.rs` 的 `is_number` 使用 ICU4X `GeneralCategory::{DecimalNumber, LetterNumber, OtherNumber}` | 已有等价能力。P0-2 可复用该 helper，无需移植 Kotlin 生成范围表。 |
| `PunctuationClass.Quote` 删除：弯引号已在 classifier 阶段解析为 Opening/Closing，不会产生 Quote | Rust `PunctuationClass::Quote` 仅在 `clreq_profile.rs` 禁则和 `width_independent_annotation_cache.rs` 的样式匹配中出现；当前 `classify` 不返回它 | 归入 P1；此项是 Kotlin `76537615` 的死分类删除，未引入标点规则。实施前须确认公开 API 变更。 |
| `CjkDashCapabilityPolicy` | Rust 现有 dash ink rollback 是本地 shaping 行为；Kotlin policy 是 host async probe 的具名 capability issue | 不适用。本仓库不承载 Kotlin Web/ABI host probe 协议。 |
| `InlineShapingStylePolicy`：16 个 CSS inherited property 的首个差异降级 | Rust 没有 DOM/CSS lowering 或 Kotlin ABI 输入 | 不适用，属于 Kotlin Web 宿主接口规则。 |
| `FontPolicy` 文字说明 | Rust font classifier 的 dash/ellipsis 基础角色仍为 CJK，需由 paragraph-level resolver 覆盖 | 归入 P0-1，无独立改动。 |

C1 的 Quote enum 核对结果：Rust `PunctuationClass::Quote` 是公开变体，虽 `classify()` 永不返回，
但 `punctuation_glue_placement_test.rs` 仍直接传入它，且 production 中保留两处不可达 match arm。
Kotlin `76537615` 删除的是从不产生的分类。Rust 若同步删除，需要改动公开 enum 和该测试的人工
输入矩阵；这是可独立验证的 API 清理，不影响用户可见布局，列为 P1。

C1 调查结论：生产相关实现已核对；测试函数映射仍在进行。Unicode number 与 host policy 不新增 Rust
实现；Quote enum 清理列为 P1；排版差异集中在 P0-1 与 P0-2。

### 来源 C / D：fixture、golden 与 shaping evidence 协议

Kotlin `LayoutFixtures.kt` 在本区间新增三项行为 fixture：`contextual-dash-ellipsis`、
`parenthetical-dash-pairs` 与 `quote-digit-boundaries`。`FixtureJsonMain.kt` 将已解析的 fixture
转为 JSON；Rust `examples/fixture-layout-dump.rs` 的 `FixtureWire` 已读取这三个 fixture 所需的全部
字段（文本、constraints、line break spans、grid、ruby、decorations、hyphenator 和 kinsoku pin），
因此普通 stub fixture 不需要 schema 改动。

| 上游协议与资产 | Rust 现状 | 结论 |
| --- | --- | --- |
| `golden/layout-dumps/`：deterministic stub dump | `tools/verify-fixture.sh` 通过 Kotlin `exportLayoutFixture` 喂给 Rust runner，并逐字 diff 同一路径 golden | 本调查时固定列表尚遗漏本区间新增的三个 fixture；P0-1/P0-2 已将其加入，当前清单为 52 项。 |
| `shaping-evidence.json`：TextShaper/FontMetrics 的请求键、glyph、ink bounds、HALT 与决策记录 | Rust 无 JSON parser、recorded `TextShaper`、metrics resolver 或 recorded dump runner | 归入 P2。Kotlin 明确把该 JSON 定义为语言中立的跨引擎 conformance artifact；完整跟进需先在 Rust 实现 strict replay（缺键失败），再比较 `layout-dumps-recorded/`。 |
| `layout-dumps-recorded/`：Skia/HarfBuzz evidence 回放后的 53 份 fixture dump | Rust 只消费普通 `layout-dumps/`，未消费 recorded 目录 | 归入 P2；recorded 字体 dump 不能直接替换 stub golden。 |

协议事实：Kotlin recorder 只在 JVM/Skia 上写入 evidence，检查入库的 JSON 与 recorded dump 可在所有 Kotlin target
回放；Rust 可消费同一 JSON，但其 `FontDecision`、`ShapingInput` 和 debug dump 字段必须先精确映射。
因此该项工作包含 JSON 回放和跨语言 conformance 基础设施，不能仅复制 59 个 golden 文件。

C4/D 调查结论：协议已核对，测试函数映射仍在进行。普通 fixture 清单归入 P0-1/P0-2；shaping
evidence replay 归入 P2，避免在 stub 与实测 shaping 两种基线之间混淆失败原因。

### 来源 B / C：断行、行调整与 cache

Kotlin `5d834ecb` 将所有不可断 cluster interval 封装为 `UnbreakableRanges`：用按 start 排序和
prefix-max-last 索引在 $O(\log n)$ 判断 boundary/包含关系，找到命中后才按原列表顺序扫描以保留
fixed-point repair 的既有优先级。`LineBreakPlanningStage` 另为 line-break spans 建立同型 overlap index；
`PunctuationGeometryLedger` 则在“cluster source 有序且不重叠”的既有不变量下对 covered range 两端
二分搜索。上游动机是消除标点密集、超长 token 上每候选 boundary 全表扫描导致的二次复杂度。

| Kotlin 变更 | Rust 现状 | 结论 |
| --- | --- | --- |
| `UnbreakableRanges` 及其在 greedy、lookahead、repair、DP 的统一调用 | Rust `line_break_planning_stage.rs` 聚合 `Vec<IntRange>`；`paragraph_dp_line_breaker.rs` 与 `progressive_break_decisions.rs::adjust_break_for_unbreakables` 仍 `.iter().find(...)` | 归入 P3。语义保持原 scan priority，适合在 benchmark 证明瓶颈后独立移植；不与当前排版正确性修复合并。 |
| line-break span `IntervalOverlapIndex` 与 cluster range 二分 | Rust 的对应 planning/geometry 仍主要线性扫描 | 归入 P3；需要先为 Rust 构造同级病理输入与基准，避免没有测量依据的结构重写。 |
| `LineRepair` 删除冗余 null 条件、`WidthIndependentAnnotationCache` 删除未调用 helper | Rust 无对应行为差异 | 不同步。 |
| `UnicodePunctuationBoundaryResolver` 的 surrogate 分支删减 | Kotlin 已在 paragraph input 入口拒绝不配对 surrogate | 不适用。Rust `Text` 是有效 UTF-8，输入域已更严格。 |
| `ParagraphShapingStage` 用 `HashSet` 去重 emergency tracking decision | Rust 目前的同类路径已有 behavior，但本 diff 未显示新的结果语义 | 不列入实施项；只在后续 dump 差异指向重复 debug decision 时再比对。 |

#### 与 contextual dash/ellipsis 的新增下游依赖

Kotlin `ContextualPunctuationDisplaySubstitution.substituteForRole()` 在 CJK 字形替换之后检查最终 font
role：仅 `CjkPunctuation` 可保留 CLREQ display-codepoint replacement；Latin-resolved dash/ellipsis 即使
形成独立 shaping segment 也必须保留 source code point。`ParagraphShapingStage` 已调用此 helper。
Rust 当前未发现角色条件检查后的 substitutor 调用。因此 P0-1 的最小实现范围应补充：
**最终角色驱动 display substitution，classifier、cache、segmentation 与 debug 也要同步更新。**

#### Prepared plan 与数字序列化

Rust `prepared_paragraph.rs` 已有 ECMAScript-style JSON formatter，却以 Rust `f64::to_string()` 取得
有效数字长度。Kotlin 本区间将 `ecmaJsonNumber` 改为从 f64 bits、相邻 round-trip interval 和
half-even exact decimal search 导出最短 digit count，再用 f32 grid 重建 digit content；相关
`PreparedParagraphJsonNumberTest` 覆盖 powers of two、subnormal、临界 rounding 和固定 raw bits。

这项差异属于 plan/FFI 的字节 conformance，不改变 Rust 的普通布局计算。Rust 同时尚未有 Kotlin 新增的
`renderEvidence` JSON：glyph、font instance、ruby/bopomofo、interlinear segment、emphasis dot 等渲染
回放字段。两者在来源 E 与 prepared-plan API 一起判断；不要把 render-evidence 缺失误判为 layout
algorithm 差异。

C3 调查结论：生产相关实现已核对，测试函数映射仍在进行。性能索引归入 P3；prepared JSON 输出归入
P0-3；当前字符布局相关的 display-substitution gate 归入 P0-1。

`LineBreakPlanningStage` 的完整 diff 已复核：除前述 interval index 与 `UnbreakableRanges` 外，唯一
改动是预先标记可 emergency-track 的 cluster boundary，再按 decision 的 source 顺序填充 reason，避免
每一个 boundary 扫描整个 decision list。`put` 仅在 reason 未设置时写入，保持原先 `firstOrNull` 的
decision-list 优先级。因此本文件没有额外的断行正确性同步项。

#### 角色规则实施项的精确回归矩阵

Kotlin 的定向测试已确认下列最小断言；Rust 实施时应按这些可解释输出写测试，并检查最终几何。

| 同步包 | 输入与断言 |
| --- | --- |
| contextual dash/ellipsis | `English` 两侧的 U+2014、`A`/`B` 两侧的连续 U+2014、`Wait`/`what` 两侧的连续 U+2026 解析为 `LatinText`，source 为 `DashEllipsisSurroundingScriptContext`，display 保留 source；中文两侧的 U+2014/U+2026 解析为 `CjkPunctuation` 并使用相应 CJK display；冲突文本与孤立 U+2026 走 `ParagraphLanguageDashEllipsisContext`；换行中断脚本查找。 |
| parenthetical dash pair | `Jessica` 插入语前后的等长 U+2014 run 必须同为 CJK 并替换为 `⸺`，reason 前缀为 `parenthetical-pair-conflicting-outer-script`；Latin 插入语前后的 run 同为 Latin，source 为 `ParentheticalDashPairContext`；标点、symbol、不同 run length、ellipsis 或 mandatory break 均不得配对。 |
| role-gated substitution 与 segmentation | Latin U+2026 在 `en-US` 的 role 为 Latin、display 保持 U+2026，substitution reason 为 `CjkRoleGatedDisplaySubstitution:preserve-role-LatinText`；Latin U+2014/U+2026 run 为独立 cluster，且不产生 CJK punctuation decision。 |
| quote numeric boundary | `尾号是“1‘2’3”。` 中 single pair 保持 CJK，source 为 `PairedPunctuationEnclosingQuoteContext`；`他用时1’30”，屏幕是6.1”的。` 的三个未配对 closing mark 均为 Latin，source 为 `NumericPrimeUnmatchedQuote`；`90’s` 仍走 `NonCjkInWordApostrophe`，以防数字边界修复回归。 |
| fixture 调用 | 上述行为对应 Kotlin fixture `contextual-dash-ellipsis`、`parenthetical-dash-pairs` 和 `quote-digit-boundaries`；Rust `verify-all-fixtures.sh` 当前未列出任一 id。 |

这组矩阵确认：dash/ellipsis、parenthetical pair、role-gated substitution、standalone segmentation 与三份
fixture 需要作为同一行为包同步；数字 apostrophe 与 numeric prime 可作为较小的独立 quote 修复包。

### 来源 E：平台、Native 与 ABI

Kotlin `tiqian_font_backend.h` 定义 protocol revision 2 的 Kotlin/Native 宿主字体 vtable：host 通过
packed shape buffer 和固定 metrics record 回调 Kotlin/Native static library。`PlanPackedWriter.kt` 是同一
产品流程的 revision 1 little-endian packed plan writer，并且由 Kotlin native 侧提供 dump entry。它们与
ADR 0050 的 Kotlin Native / Rust **precompute binding** 边界一致，不属于独立 `tiqian-rs` layout crate 的
ABI。

Rust 源树没有 `extern "C"`、`no_mangle`、C header 或 packed plan writer；只公开 Rust 内部的
`to_prepared_paragraph_json(&LayoutResult, render_evidence)`。因此：

- Native font vtable 的 family-array typing、protocol revision 2、session callback 和 buffer retry：不适用。
- packed binary plan 的 magic/revision/column layout：不适用，除非未来用户明确要求 Rust 实现成为 Kotlin
	Native host 或 precompute ABI producer。
- Kotlin `renderEvidence` 新字段、JSON formatter 和 Rust JSON plan 的字段映射：归入 P0-3，属于
	JSON public output，不应通过复制 Kotlin packed buffer 实现来解决。

调查结论：Native ABI 边界已确认，相关测试函数映射仍在进行。P0-3 已确定 Rust JSON 与 Kotlin
`toPreparedParagraphJson(renderEvidence = false/true)` 对齐；以 Kotlin `PreparedParagraphJsonNumberTest`
和 render evidence 测试建立字段断言，不引入 Kotlin Native ABI。

## 实施工作项与验证

本节保存 P0–P3 的范围与验证证据；状态只在“统一工作项状态”表更新。生产引擎审计已确认的项目列在 P0；
测试覆盖映射完成后，将对应测试函数附入本节或“测试映射工作项”的映射记录。

| 顺序 | 实施项 | 范围 | 已确认验证 |
| --- | --- | --- | --- |
| P0-1 | contextual dash/ellipsis | 已翻译 resolver、parenthetical pair、quote-aware cache 后的 dash-aware classifier、role override debug、Latin run segmentation、role-gated display substitution；已将 `contextual-dash-ellipsis` 与 `parenthetical-dash-pairs` 加入 fixture 校验清单 | 2026-08-31：6 项定向回归、`cargo test` 275 项、51 份 fixture golden 全部通过。 |
| P0-2 | 数字引号与 prime | 已翻译数字-数字 U+2019 不被 apostrophe 吸收、紧邻数字的未配对 U+2019/U+201D 的 Latin prime 决策，以及 Kotlin `isLatinCodePoint` 的固定码点范围；已在 fixture 校验清单加入 `quote-digit-boundaries` | 2026-08-31：classifier 12 项、quote analyzer 10 项、quote layout 23 项定向回归，`cargo test` 282 项、52 份 fixture golden 全部通过。 |
| P0-3 | prepared JSON 输出 | 已将 Rust API 收敛为 `to_prepared_paragraph_json(&LayoutResult, render_evidence)`；已翻译 exact float round-trip JSON number formatter、cell/paragraph evidence 字段和 ruby ascent | 2026-08-31：Kotlin 全部 number vectors、plain/evidence 与 cell/paragraph evidence 3 项定向回归，`cargo test` 285 项、52 份 fixture golden 全部通过。 |
| P1 | `PunctuationClass::Quote` 清理 | 经确认删除 classify 永不产生的公开 enum variant、production 不可达分支与人工构造测试输入 | 2026-08-31：glue placement 5 项定向回归，`cargo test` 285 项、52 份 fixture golden 全部通过。 |
| P2 | recorded shaping evidence replay | Rust 开发用 fixture runner 通过 `TIQIAN_SHAPING_EVIDENCE` 严格回放 Kotlin `shaping-evidence.json`，并由 `verify-recorded-fixtures.sh` 比对 Kotlin `layout-dumps-recorded/`；资源仍由相邻 Kotlin checkout 提供，不复制进 Rust 仓库 | 2026-08-31：52 份 corpus 全部逐字匹配。首次发现的 4 处差异均为 dump 一位小数的 half-up 格式化不一致，已按 Kotlin `Float.dumpFmt()` 翻译；未发现对应布局算法差异。 |
| P3 | pathological-token 性能索引 | 已翻译 `UnbreakableRanges`、interval overlap、source-order cluster range binary search 与 80k giant-token 热路径优化 | 2026-09-02：索引语义 3/3、受影响既有回归 78/78 通过；完整借用型 `JustificationRequest` 移除逐行边界集合 clone 后，release 80k 强制回归为 425.711ms，通过 1500ms 上限。 |

明确不同步：Kotlin Web CSS shaping policy、CJK dash host capability naming、Kotlin Native font vtable、
packed plan ABI、平台 trace 适配，以及仅为 Kotlin valid-UTF-16 检查而删除的 surrogate 防御分支。

#### P2 recorded evidence 基线（2026-08-31）

Rust 的 `tools/verify-recorded-fixtures.sh` 沿用普通 fixture 校验的 `TIQIAN_ROOT` 约定：每份输入仍由
Kotlin `:engine:exportLayoutFixture` 导出，evidence JSON 与 recorded golden 直接从 Kotlin checkout 读取。
runner 仅在设置 `TIQIAN_SHAPING_EVIDENCE` 时使用记录的 shaper 与 metrics resolver；每个 request 都按
Kotlin `ShapingEvidenceKey` / `MetricsEvidenceKey` 全字段查找，缺键立即失败。默认 fixture runner 继续走
deterministic stub，不受该路径影响。

首次完整 corpus 检查曾有 4 项首处差异。它们跨越标点 geometry、两端对齐和 Latin shaping 三条路径，
但首处数值均落在一位小数的 $n+0.25$ tie：Kotlin `Float.dumpFmt()` 按幅值 half-up，Rust
`format!("{value:.1}")` 在这些 tie 输出为 half-even。将 runner 的 formatter 按 Kotlin 翻译后，52 个
fixture 均逐字匹配；下表保留初始观测及原因，避免以后把同类输出差异误判为布局算法分叉。

| fixture | 首处差异 | 已验证分类 |
| --- | --- | --- |
| `adjacent-curly-quote-list-context` | 四个 opening curly quote 的 consumed leading glue：Kotlin `1.3`，Rust `1.2`；body、natural glue 与 resolved advance 相同 | dump half-up 格式化 |
| `justify-unbreakable-number-symbol` | justify 后 cluster `文` advance：Kotlin `21.3`，Rust `21.2` | dump half-up 格式化 |
| `latin-camelcase` | 段落 size / natural width：Kotlin `121.3`，Rust `121.2` | dump half-up 格式化 |
| `latin-hard-break` | `ork` cluster 与第二行 width：Kotlin `25.3`，Rust `25.2` | dump half-up 格式化 |

#### P3 性能索引实施与验证（2026-09-02）

Rust 已按 Kotlin `UnbreakableRanges` 的语义实现按 start 排序与 prefix-max-last 索引；命中后仍按原始 range
列表顺序返回，保留 contiguous fixed-point repair 的既有优先级。Greedy、Lookahead、repair 与 paragraph-DP 均改用
该查询对象。`LineBreakPlanningStage` 的 technical overlap 与 emergency tracking，以及
`PunctuationGeometryLedger` 的 source-range cluster 查询，也已分别改为 Kotlin 同型的区间索引和二分查询。

新增 `tests/layout/unbreakable_ranges_test.rs` 的 3 个独立测试验证未排序/嵌套 range、原列表优先级与固定点退回；
`inline_object_layout`、`line_adjustment_push_in`、`line_repair_coverage`、`lookahead_line_breaker`、
`paragraph_dp_line_breaker`、`push_in_line_wide_capacity` 六组受影响回归共 78 项通过。

Kotlin `GiantTokenScalingProbe` 的两个函数也已分别翻译为
`tests/layout/giant_token_scaling_probe_test.rs` 的独立 Rust 测试。Rust 没有 AWT shaper，故该探针只衡量 Rust
stub pipeline，不能与 Kotlin AWT 时间直接比较。定位确认瓶颈在行调整阶段逐行 clone 段落级边界集合，尤其是
80k emergency tracking map。`JustificationRequest` 保持完整请求对象，其 map/set 字段改为借用；生产调用点和测试
helper 均先配置拥有的集合、后构造完整请求。2026-09-02 的 release 验证中，80k 单一 token warm layout 为
425.711ms，通过 1500ms 上限。实验矩阵中 80k warm 时间为 lookahead 454.137ms、greedy 447.506ms；两种策略均未
恢复逐行复制造成的近二次增长。

## 测试映射工作项

本节是 T1–T5 的唯一映射记录入口。每个工作项完成时，需将该项的 Kotlin 测试函数、Rust 对应测试、
“已等价”“需补测试”“需同步实现”或“不适用”结论，以及实际验证追加到对应小节。

| 工作项 | 当前输入范围 | 现有事实 | 首个待审主题 |
| --- | --- | --- | --- |
| T1 | 92 个 fixture、golden、evidence、trace 与 JVM 工具资产 | G1–G5 已完成函数或资产级映射；prepared serializer 的 22 个映射测试和 diagnostics API 已迁移并通过全量 Rust 验证。 | T2 开始前保持冻结。 |
| T2 | 35 个 CLREQ、core、font、linebreak 测试资产 | 已核对 `UnicodeNumber`、Quote enum 与部分 host policy。 | `ClreqProfileCoverageTest`、`FontPolicyCoverageTest`、`EastAsianSpacingCoverageTest`。 |
| T3 | 24 个标点 geometry 与行内布局测试资产 | recorded corpus 覆盖 recorded shaping 主路径，尚未覆盖所有 HALT/ink 分支。 | `PunctuationAtomBuilderHaltTest`、`PunctuationGeometry*CoverageTest`、`R3GeometryTailCoverageTest`。 |
| T4 | 22 个 shaping、role/cache、quote/dash 与 Unicode 边界资产 | P0-1/P0-2 已覆盖新增 dash、ellipsis、quote、prime 主行为。 | `ParagraphShapingStageCoverageTest`、`WidthIndependentAnnotationCacheCoverageTest`、`UnicodePunctuationBoundaryResolverCoverageTest`。 |
| T5 | 35 个 breaker、repair、adjustment 与 hyphenation 测试资产 | P3 已确认属于性能索引候选；其余新增边界尚未逐函数映射。 | `LineAdjustmentStageCoverageTest`、`LineRepair*Test`、`ParagraphDp*CoverageTest`。 |

### T1：fixture、golden、recorded evidence、trace 与 JVM 工具

T1 的 92 项输入由 `tools/list_kotlin_test_audit.py --group fixtures-and-recorded-evidence --paths`
锁定。以下五组按路径互不重叠，数量合计 92；每次只处理一组，并在该组表格中追加函数级映射。

| T1 组 | 资产数 | Kotlin 输入 | 审计目标 | 状态 |
| --- | ---: | --- | --- | --- |
| G1：stub fixture 与 decision dump | 10 | `LayoutFixtures`、`LayoutDumpFormat`、common/JVM golden tests、6 份 `layout-dumps/` | fixture JSON 输入、三种 breaker dump、golden 更新与跨 target parity。 | 完成 |
| G2：recorded shaping evidence | 57 | `ShapingEvidence*`、recorded parity/recorder、`shaping-evidence.json`、52 份 `layout-dumps-recorded/` | request key、JSON wire、严格回放与 recorded dump parity。 | 完成 |
| G3：prepared plan 与 render evidence | 5 | 4 个 common prepared tests、`PreparedParagraphRenderEvidenceTest` | plain/evidence append-only 规则、plan fields 与渲染证据。 | 完成 |
| G4：trace 基础设施 | 18 | common trace helpers/scenarios、4 个 platform actual、`TraceGoldenTest` | trace wire、断言、平台适配与 golden 的 Rust 适用范围。 | 完成 |
| G5：JVM 诊断工具 | 2 | `GiantTokenScalingProbe`、`LayoutReportMain` | 性能探针和 report 是否定义 Rust 需要同步的可观察输出要求。 | 完成 |

G1 的实际输入为 4 个 Kotlin 源文件和 6 份普通 golden；G2 的 52 份 recorded golden 加上 evidence JSON
与 5 个 recorder/replay 源文件构成 57 项。该数量分解防止 golden 同时归入两个工作组。

#### G1：stub fixture 与 decision dump 映射

本组只核对 deterministic stub 的语言中立 dump 协议。Kotlin 的 fixture exporter
`FixtureJsonMain.kt` 虽不在本次变更资产中，仍是 Rust runner 的输入边界：它直接导出已经解析的
`LayoutInput`，使 Rust 使用 Kotlin fixture 解析后的默认值。

| Kotlin 函数或资产 | Rust 对应物 | 结论 | 验证证据 |
| --- | --- | --- | --- |
| `EarlyLayoutFixtures.all`（52 个 fixture） | `tools/verify-all-fixtures.sh` 的 52 个 id；每项由 `verify-fixture.sh` 经 Kotlin exporter 输入 `fixture-layout-dump` | 已等价 | 2026-08-31 的 stub corpus 为 52/52 byte-for-byte matched；本轮未改布局、fixture、golden 或 runner，未重跑。 |
| `layoutFixtureDump(fixture, textShaper, fontMetricsResolver)` | `examples/fixture-layout-dump.rs` 的 `main`、`layout`、`to_input` 与 `dump_result` | 已等价 | 两侧均固定依次运行 greedy、lookahead、paragraph-DP；Kotlin exporter 传递 text、source boundaries、constraints、grid、indent、line-break span、decoration、ruby、hyphenation 与 kinsoku pin。当前 fixture 未含 text span、inline box 或 inline object；Rust 对这三类非空输入显式拒绝。 |
| `LayoutResult.decisionDump(label)` | `dump_result(label, result)` | 已等价 | Rust 输出顺序和标签逐项翻译 Kotlin；既有 52/52 byte diff 同时覆盖可观察字段顺序、空字段省略和三种 breaker 的结果。 |
| `Float.dumpFmt()`、`String.escapeDumpText()` | `fmt`、`escape` | 已等价 | `fmt` 保留 NaN、无穷、负零和按幅值 half-up 的一位小数；P2 曾以 4 个 $n+0.25$ 差异定位该协议，翻译后 recorded corpus 52/52 匹配。文本控制符转义逐项对应 Kotlin。 |
| `layoutDumpDiffMessage(id, expected, actual)` | `tools/verify-fixture.sh` 的 `diff -u` 与首个不同 dump 行提示 | 已等价 | 两者均提供 fixture id 和可读的按行差异；诊断呈现不同，不属于 dump wire。 |
| `LayoutDumpGoldenTest.layoutDecisionDumpsMatchGolden()` | `tools/verify-fixture.sh`、`tools/verify-all-fixtures.sh` | 已等价 | Kotlin JVM 唯一负责写入/更新 `golden/layout-dumps/`；Rust 只读取 Kotlin checkout 的同一文件并比较，不拥有更新路径。既有 stub 52/52 记录见上。 |
| `LayoutDumpGoldenParityTest.layoutDecisionDumpsMatchEmbeddedGolden()` | 无 Rust 同 target 对应测试；`verify-fixture.sh` 覆盖其语言中立 golden 子集 | 不适用 | Kotlin 测试的职责是 JVM、JS、Native 间的嵌入 golden 一致性，Rust 独立 crate 不参与 Kotlin target matrix。Rust 对同一 golden 的跨引擎比较由脚本承担。 |
| 6 份新增 `golden/layout-dumps/*.txt` | 上述固定 52-id 清单的对应项 | 已等价 | `contextual-dash-ellipsis`、`ellipsis-and-dash`、`fallback-roles`、`parenthetical-dash-pairs`、`quote-digit-boundaries`、`real-paragraph-1` 均已列入 Rust 清单；其布局行为的定向测试映射留给 T3–T5。 |

G1 结论：Rust 没有自有 golden，也不生成 Kotlin golden；它消费 Kotlin 导出的有效输入与 Kotlin 拥有的基线。
这保留单一 golden 写入端，并将跨语言差异限制为可逐字检查的 dump。上述验证证据均为既有结果，
本轮仅更新审计文档，未执行 fixture 或 Cargo 测试。

#### G2：recorded shaping evidence 映射

本组的 evidence 与 recorded golden 均由 Kotlin JVM/Skia 写入；Rust 仅在开发用 fixture runner 设置
`TIQIAN_SHAPING_EVIDENCE` 时读取 evidence。普通 stub 路径不加载该文件。

| Kotlin 函数或资产 | Rust 对应物 | 结论 | 验证证据 |
| --- | --- | --- | --- |
| `ShapingInput.toEvidenceKey()`、`FontMetricsRequest.toEvidenceKey()` | `ShapingEvidenceKey::from_input`、`MetricsEvidenceKey::from_request` | 已等价 | shaping key 比较 display text、candidate key/family、role、style families、size、weight、italic、locale、OpenType features 共 10 项；metrics key 比较 key、size、role、locale、families、weight、italic、face selection text 共 8 项。 |
| `RecordedEvidenceTextShaper.shape` | `RecordedEvidenceTextShaper::shape` | 已等价 | 两侧按完整 key 查询；缺 shaping entry 立即失败。命中后均重建单 cluster、单 glyph run 和完整 shaping decision 字段，并保留调用方的 source range/source text。 |
| `RecordedEvidenceFontMetricsResolver.resolve` | `RecordedEvidenceFontMetricsResolver::resolve` | 已等价 | 两侧按完整 metrics key 查询；缺 entry 立即失败。ascent、descent、leading、source、typo ascent/descent 均从记录值重建。 |
| `ShapingEvidenceJson.parse` | `RecordedShapingEvidence::load` 与 serde wire structs | 已等价 | Rust 读取 Kotlin 的 `shaping`、`metrics` entry array 及每个可观察 result 字段：advance、features、glyph id/position/bounds/HALT、decision metadata、font metrics。`meta` 只描述录制环境，未参与 replay key 或 layout 输出。 |
| `ShapingEvidenceJson.encode`、`ShapingEvidenceBuilder.record/build`、`RecordingTextShaper`、`RecordingFontMetricsResolver` | 无 Rust 对应物 | 不适用 | 这些函数的职责是 JVM/Skia 录制和写入 Kotlin 拥有的 corpus。Rust 的跨语言比较必须消费该 corpus，不能成为第二个 recorder。 |
| `ShapingEvidenceRecorderTest.recordShapingEvidence()` | 无 Rust 对应测试；Kotlin JVM test 保留唯一录制入口 | 不适用 | 此测试只有 `TIQIAN_RECORD_SHAPING=1` 才写入 `shaping-evidence.json` 与 `layout-dumps-recorded/`；正常运行显式跳过。 |
| `RecordedEvidenceGoldenParityTest.recordedEvidenceLayoutMatchesGolden()` | `tools/verify-recorded-fixtures.sh` | 已等价 | Kotlin 以嵌入资源逐 fixture 回放；Rust 脚本对 recorded golden 目录中的每个 fixture 导出 Kotlin input、设置 evidence 环境变量并 byte diff，收集全部失败后退出。 |
| `shaping-evidence.json` 与 52 份 `layout-dumps-recorded/*.txt` | `verify-recorded-fixtures.sh`、`fixture_layout_dump/recorded_shaping_evidence.rs` | 已等价 | 2026-08-31 recorded corpus 为 52/52 byte-for-byte matched。该记录是在 formatter 按 Kotlin half-up 翻译后获得；本轮未改 evidence、runner 或布局代码，未重跑。 |

G2 结论：evidence 的可观察键、结果字段和严格缺键失败均已从 Kotlin 翻译；录制职责保留在 Kotlin JVM。
普通 stub golden 与 real-shaping recorded golden 分属 G1/G2，不能互相替换。

#### G3：prepared plan 与 render evidence 映射（实施范围待定）

Kotlin 的五个测试文件已逐函数读取。Rust `prepared_paragraph.rs` 已实现 plain plan 与
`render_evidence` 路径中的主要字段；现有 `prepared_paragraph_test.rs` 覆盖数值向量、append-only、
synthetic cell/paragraph evidence 以及实际 ruby/Bopomofo layout。以下结论只引用现有源码和既有验证，
不表示 G3 的待补测试和 diagnostics API 已实施。

| Kotlin 测试函数 | Rust 对应物 | 结论 | 现有证据或待补范围 |
| --- | --- | --- | --- |
| `PreparedParagraphJsonNumberTest.zeroValuesSerializeWithoutSign` | `ecma_json_number_matches_kotlin_vectors` | 已等价 | 同时覆盖 `0`、`-0`、`NaN`、两种 infinity 的精确字符串。 |
| `PreparedParagraphJsonNumberTest.integerFormsPadToDecimalExponent` | `ecma_json_number_matches_kotlin_vectors` | 已等价 | 同时覆盖 `1`、`200`、$10^{15}$、$10^{16}$、$10^{20}$ 与 $2^{53}$ 向量。 |
| `PreparedParagraphJsonNumberTest.fractionFormsInsertDecimalPoint` | `ecma_json_number_matches_kotlin_vectors` | 已等价 | 同时覆盖 `1.5`、`12.5`、`1000000.5`。 |
| `PreparedParagraphJsonNumberTest.smallFractionsUseLeadingZeros` | `ecma_json_number_matches_kotlin_vectors` | 已等价 | 六个固定小数向量与 Kotlin 预期逐项相同。 |
| `PreparedParagraphJsonNumberTest.exponentFormsCarryExplicitSign` | `ecma_json_number_matches_kotlin_vectors` | 已等价 | 五个大数和两个小数指数向量逐项相同。 |
| `PreparedParagraphJsonNumberTest.negativeValuesKeepOnlyMagnitudeSign` | `ecma_json_number_matches_kotlin_vectors` | 已等价 | 覆盖 `-1.5` 与负指数小数。 |
| `PreparedParagraphJsonNumberTest.exactTiesRoundToEvenDigit` | `ecma_json_number_matches_kotlin_vectors` | 已等价 | 三个 half-even / 非零尾数向量逐项相同。 |
| `PreparedParagraphJsonNumberTest.exactExpansionRoundsPlatformDigits` | `ecma_json_number_matches_kotlin_vectors` | 已等价 | $2^{60}$、$2^{-44}$、$2^{122}$ 向量逐项相同。 |
| `PreparedParagraphJsonNumberTest.boundaryMidpointsAcceptOnlyAtEvenMantissa` | `ecma_json_number_matches_kotlin_vectors` | 已等价 | 两个 Kotlin raw-bit midpoint 向量逐项相同。 |
| `PreparedParagraphJsonNumberTest.decimalAlignedMantissaSkipsZeroChunk` | `ecma_json_number_matches_kotlin_vectors` | 已等价 | `12500000` 向量相同。 |
| `PreparedParagraphJsonNumberTest.subnormalExpansionsSerialize` | `ecma_json_number_matches_kotlin_vectors` | 已等价 | `f32::from_bits(1)` 和 `f32::from_bits(3)` 向量相同。 |
| `PreparedParagraphInlineEdgesTest.endOnlyInlineBoxEmitsEdgeWithoutInlineStartField` | `append_paragraph_render_evidence` | 需补测试 | Rust 仅在 `inline_end_by_offset` 有值时输出 `inlineEnd`，但现有测试未固定 end-only 和省略 `inlineStart`。 |
| `PreparedParagraphInlineEdgesTest.contentWithoutInlineBoxesOmitsInlineEdgesArray` | `append_paragraph_render_evidence` | 需补测试 | Rust 的两个 edge map 都为空时省略数组，但现有测试未固定无 box 输入。 |
| `PreparedParagraphJfTest.styleAtAndStyleDeltasInPreparedParagraphJson` | `style_at`、`append_cell_render_evidence` | 需补测试 | Rust 反向查找 span 并输出 paint 字段；未测试重叠 span 的最后覆盖规则。 |
| `PreparedParagraphJfTest.inlineBoxEdgesAndEmphasisDotsFilter` | `append_paragraph_render_evidence` | 需补测试 | Rust 有相同 edge/dot filter；现有 synthetic evidence 同时含有效 start/end 和有效 dot，未覆盖被过滤的三类 dot。 |
| `PreparedParagraphJfTest.dashShapingDecisionWithGlyphIds` | `append_cell_render_evidence` | 需补测试 | Rust 输出 strategy、language、face、glyph IDs 与 reason；现有测试覆盖同字段，但未固定双 glyph 合计 natural width。 |
| `PreparedParagraphJfTest.ecmaJsonNumberEdgeCases` | `ecma_json_number_matches_kotlin_vectors`、`ecma_json_number` | 需补测试 | Rust 已固定 Kotlin 精确向量；缺 Kotlin 的 subnormal、powers-of-two 与 2000 次数值 sweep。 |
| `PreparedParagraphPlanConstructionTest.openTypeFeaturesAndRenderFontFamilyAttachPerCluster` | `to_prepared_paragraph_json` | 需补测试 | Rust 按 cluster 聚集 feature 并附 render family；现有测试只间接检查 feature 和 family。 |
| `PreparedParagraphPlanConstructionTest.multiUnitClusterMarksShapingBoundary` | `to_prepared_paragraph_json` | 需补测试 | Rust 有同一 range 长度判定，当前没有边界断言。 |
| `PreparedParagraphPlanConstructionTest.inlineObjectCellEmitsAdvanceOverride` | `to_prepared_paragraph_json`、`append_cell_render_evidence` | 需补测试 | Rust 有 inline-object advance、advance override 和 evidence-only 空 display cell 分支，当前未逐项固定。 |
| `PreparedParagraphPlanConstructionTest.styleDeltaListsOnlyPaintFields` | `style_at`、`append_cell_render_evidence` | 需补测试 | Rust 仅输出 paint delta，且 non-paint-only span 输出空对象；现有测试没有该空对象断言。 |
| `PreparedParagraphPlanConstructionTest.dashClusterEmitsShapingEvidenceBlock` | `append_cell_render_evidence` | 需补测试 | Rust 有 strategy gate 和可选字段省略；现有测试未覆盖双 glyph ID 串、reason 与 summed natural width 的组合。 |
| `PreparedParagraphPlanConstructionTest.punctuationInkFloorAndLatinRoleMarkCells` | `append_cell_render_evidence` | 需补测试 | Rust 仅在 applied 且 floor 存在时输出 ink 字段，并按 font decision 标 Latin；当前仅覆盖有效值。 |
| `PreparedParagraphPlanConstructionTest.zeroWidthBreakClusterSurvivesEmptyDisplayText` | `to_prepared_paragraph_json`、`append_cell_render_evidence` | 需补测试 | Rust 保留 zero-width decision 的空 display cell，并有无 glyph 的 shaping evidence 回退；当前未固定该组合。 |
| `PreparedParagraphPlanConstructionTest.paragraphEvidenceEmitsEverySection` | `append_paragraph_render_evidence` | 需补测试 | Rust 有 emphasis、edge 累计、ruby、Bopomofo、decoration 和 dot filter；现有测试只覆盖各 section 的简化组合。 |
| `PreparedParagraphPlanConstructionTest.negativeZeroAndExponentWidthsNormalize` | `append_json_number`、`ecma_json_number` | 需补测试 | 实现将任意零规范化为 `0`；尚无 line/paragraph geometry 字段中的负零与指数断言。 |
| `PreparedParagraphPlanConstructionTest.jsonStringEscapesQuotesBackslashesAndControlCharacters` | `append_json_string` | 需补测试 | Rust 分支覆盖引号、反斜线、五种命名控制字符和 U+0001；无对应回归。 |
| `PreparedParagraphPlanConstructionTest.planWithDiagnosticsListsCapabilityIssuesAndAdvanceSuspects` | 无 | 需同步实现 | Kotlin `toPlanWithDiagnosticsJson(renderEvidence, zeroAdvanceEpsilonPx)` 输出 escaped plan、capability issue 与 zero/NaN/infinity advance suspect；Rust 没有 envelope API。 |
| `PreparedParagraphRenderEvidenceTest.plainParagraphEvidenceIsAppendOnly` | `render_evidence_is_append_only_and_emits_kotlin_cell_and_paragraph_fields` | 已等价 | 现有测试断言 evidence 以 plain 去末尾 `}` 的前缀开始，并检查基础段落字段。 |
| `PreparedParagraphRenderEvidenceTest.pinyinRubyEmitsRubyDecisions` | `real_ruby_and_bopomofo_layouts_emit_evidence_only_when_requested` | 需补测试 | Rust 用 stub layout 检查 plain gate、ruby section 和 ascent；尚无 Kotlin 的 range、text、center/baseline、font size/weight 字段断言。 |
| `PreparedParagraphRenderEvidenceTest.bopomofoRubyEmitsBopomofoDecisions` | `real_ruby_and_bopomofo_layouts_emit_evidence_only_when_requested` | 需补测试 | Rust 用 stub layout 检查 plain gate、section 和 placement；尚无 Kotlin 的 role 字段以及 Bopomofo 不产生 ruby section 的断言。 |
| `PreparedParagraphRenderEvidenceTest.decorationsEmitSegmentsDotsAndRanges` | `append_paragraph_render_evidence` | 需补测试 | Rust 已过滤 ProperNoun/BookTitle segment 和有效 emphasis dot；现有 synthetic evidence 未验证 plain gate 与实际 decoration layout。 |
| `PreparedParagraphRenderEvidenceTest.styleDeltaEmitsPerCellStyleBlock` | `append_cell_render_evidence` | 需补测试 | Rust synthetic evidence 覆盖三个 style delta 字段；尚无 stub layout 的 plain gate 与 span style block。 |
| `PreparedParagraphRenderEvidenceTest.inlineBoxesEmitInlineEdges` | `append_paragraph_render_evidence` | 需补测试 | Rust synthetic evidence 覆盖同一 box 的 start/end 字段；尚无 stub layout 的 plain gate 与两个 offset。 |

##### G3 迁移结果（2026-09-01）

在 T1 的完整映射后，Rust 新增 `to_plan_with_diagnostics_json(result, render_evidence,
zero_advance_epsilon_px)`，按 Kotlin `toPlanWithDiagnosticsJson` 输出 escaped plan、capability issue 和
zero/NaN/infinity advance suspect。non-finite advance 同样使用 Kotlin-compatible `NaN`/`Infinity` 文本。

其余 22 个函数均按 Kotlin 函数边界迁移为 Rust snake_case regression test：inline edge 2 项、JF 4 项、
plan construction/diagnostics 11 项、stub layout render evidence 5 项。新增模块为
`prepared_paragraph_inline_edges_test.rs`、`prepared_paragraph_jf_test.rs`、
`prepared_paragraph_plan_construction_test.rs` 与 `prepared_paragraph_render_evidence_test.rs`；原有
`prepared_paragraph_test.rs` 保留 number vector、append-only 和基础 ruby/Bopomofo 覆盖。

2026-09-01 验证：四个新增模块定向运行共 22/22 通过；`cargo test --test tiqian prepared_paragraph` 与
完整 `cargo test` 均通过，后者为 307 passed、0 failed。构建仅报告既有
`paragraph_dp_line_breaker.rs` 的 `gap_boundaries` dead-code warning。

#### G4：trace 基础设施映射

G4 是 Kotlin 供 Haxe port 使用的测试观测协议，与 layout、fixture、prepared plan 的公开 wire 分离。Rust 当前
没有 trace runner 或 process-trace golden；跨引擎可观察基线已由 G1 的 layout dump 和 G2 的 recorded evidence
承担。因此不为 Rust 创建第二个 recorder、平台 actual 或 golden 写入端。scenario 覆盖的具体 CLREQ、core、font、
layout、linebreak 与 shaping 行为仍分别留在 T2–T5 逐函数映射。

| Kotlin 函数或资产 | Rust 对应物 | 结论 | 现有证据或范围 |
| --- | --- | --- | --- |
| `TraceFormat.i/f/fd/d/value/escapeText`、`TraceRecorder.event/raw/text` | 无 | 不适用 | 仅格式化 Kotlin/Haxe 测试事件；Rust fixture dump 使用独立且已验证的 `fmt` 与 `escape` 协议。 |
| `TestTraceRender.escapeOperand/render/canonicalNumbers` | 无 | 不适用 | 仅把 Kotlin assertion operand 规范化为 test-trace 文本；不参与 Rust 引擎输入、布局或输出。 |
| `TestTraceStore.open/append/classText`、`TestTraceRecorder.section/record/flush` | 无 | 不适用 | 仅在 `TIQIAN_UPDATE_GOLDEN=1` 聚合 Kotlin 测试过程；JVM test runner 的实例生命周期不是 Rust 测试模型。 |
| `TracedAssertions.recordEvent`、各 `assertEquals`/`assertTrue`/`assertFalse`/`assertNull`/`assertNotNull`/`assertFailsWith`/`fail`/`assertDoesNotThrow`/`f32Literal` | 无 | 不适用 | 这些包装保持 Kotlin `kotlin.test` 断言语义并记录 Haxe replay 资料；Rust 保持标准 `assert!`/`assert_eq!`。其中 `f32Literal` 的跨 Kotlin target 修正不改变 Rust 的 f32 语义。 |
| `TestTracePlatform.updateMode/doubleArithmetic/writeGolden` 的 JVM actual | 无 | 不适用 | JVM 是唯一可在 update mode 写 `src/jvmTest/resources/golden/test-traces` 的 Kotlin target。 |
| `TestTracePlatform` 的 Android、JS、Native actual | 无 | 不适用 | 三者均为 compare-only；JS 的 `doubleArithmetic=true` 只选择 Kotlin/JS 断言预期。 |
| `TraceGoldenTest.writeProcessTraces` | 无 | 不适用 | 仅在 JVM update mode 为 Kotlin scenario corpus 写 `golden/process-traces/<id>.txt`，供 Haxe diff。 |
| `TraceScenarios.cornerRadii/richTextPaintValidation/punctuationAdvance` | T2/T3 后续行为映射 | 延后至 T2/T3 | 场景是 Haxe 复放用的多行为组合；其中 core rich-text 规则与 CLREQ punctuation advance 将在所属测试组逐项对照。 |
| `ClreqTraceScenarios.bopomofoParser/punctuationClassify/kinsokuPolicies/punctuationWidthPolicy` | T2/T3 后续行为映射 | 延后至 T2/T3 | 每项复用已有 Kotlin 覆盖矩阵；不以 scenario 文字替代 Rust 的函数级测试映射。 |
| `CoreTraceScenarios.eastAsianSpacing/interactionBoundaries` | T2/T4 后续行为映射 | 延后至 T2/T4 | East Asian spacing、有效 UTF-16 surrogate 和 grapheme interaction boundary 分属后续 core / Unicode 审计；Rust 输入仅接受有效 UTF-8。 |
| `FontTraceScenarios.roleClassifier/metricsNormalizer` | T2/T4 后续行为映射 | 延后至 T2/T4 | font role 与 metrics normalization 均有其对应的 Kotlin 单元测试文件，不能由 process trace 判定 Rust 等价。 |
| `LayoutTraceScenarios.quoteRoleAnalyzer/stubPipeline` | T4/T5 后续行为映射 | 延后至 T4/T5 | quote analyzer 与整段 stub pipeline 的字段将由 role/linebreak 测试函数映射；G1/G2 fixture 已覆盖跨引擎 dump。 |
| `LinebreakTraceScenarios.punctuationClass/englishHyphenation` | T2/T5 后续行为映射 | 延后至 T2/T5 | UAX#14 punctuation class 与英文连字符规则须对照各自 Kotlin 单测，不复制 Haxe scenario runner。 |
| `ShapingTraceScenarios.replayableContracts` | T4 后续行为映射 | 延后至 T4 | replayable font contract 由 Kotlin shaping 测试和 G2 recorded evidence 分别覆盖；无独立 Rust trace 协议。 |

G4 结论：所有 trace 资产均已定位到 Kotlin/Haxe 测试基础设施或后续 T2–T5 的行为审计，没有新增 Rust
实现、测试或 golden 需求。

#### G5：JVM 诊断工具映射

| Kotlin 函数或资产 | Rust 对应物 | 结论 | 现有证据或范围 |
| --- | --- | --- | --- |
| `GiantTokenScalingProbe.giantTokenLayoutStaysFarBelowTheQuadraticCeiling` | P3 的后续 Rust 压力输入和基准 | 延后至 P3 | Kotlin 使用 JVM AWT shaper 对 80k ASCII token 设 1500ms 上限，数值受 JVM、字体、硬件影响，不能移植为 Rust 固定阈值；其病理输入形状和近线性目标是 P3 的测量依据。 |
| `GiantTokenScalingProbe.measureGiantTokenScalingMatrix` | P3 的后续 Rust 测量 | 延后至 P3 | 仅在 `TIQIAN_RUN_EXPERIMENTS=1` 输出 lookahead/greedy、warm/cold cache 的尺寸矩阵；不属于 CI correctness test 或公开 wire。 |
| `LayoutReportMain.main` 与 `printFixtureDump/printEngineDump` | 无 | 不适用 | 这是 Kotlin JVM 下按 fixture 运行 AWT/Skia/stub、生成终端诊断和 HTML 的开发入口；G1/G2 的 Kotlin golden 已是 Rust 的可比较输出。 |
| `LayoutReportMain.rasterizeLayoutToPngSkia/rasterizeLayoutToPng`、`LayoutReportFontProbe`、`ShaperMode` | 无 | 不适用 | 依赖 JVM AWT、Skia、宿主字体发现与 PNG 生成。Rust 当前无同源 renderer；强行生成第二份像素报告会偏离单一可观察基线，也不会增加 layout conformance。 |
| `LayoutReportMain` 的 DP comparison 数据与 `DpComparisonItem.divergence` | T5 的后续 line-break 行为映射 | 延后至 T5 | 该人工并排视图只辅助 Kotlin 调优；其断点/行内调整行为须由 T5 的 Kotlin 单测和 G1/G2 fixture 映射判定。 |

G5 结论：两个资产均为 JVM 开发诊断工具。性能输入和目标已归入 P3 的未来测量，HTML/raster/report 不产生
Rust 同步需求。至此 T1 的 G1–G5 已完成函数或资产级映射；T2–T5 继续按各自行为主题审计。

后续方法：每次选择一个主题，先取得 Kotlin 测试函数索引和相关 source diff，再按函数读取最小片段；每条
行为写入本文件的 Kotlin→Rust 对照表，并标记为已同步、待同步、不适用或仅覆盖分支。禁止用测试文件数量、
coverage 命名或 fixture 清单推断行为等价。

当前状态见“统一工作项状态”表：文件清单与部分高优先级规则已完成，T1 已完成 G1–G5 映射，T2–T5 尚未开始。未更新
`docs/tracking.md`，因为尚未选择或完成实际 Rust 同步。

### T2：CLREQ、core、font、Unicode 与 linebreak 基础行为

T2 输入由 `tools/list_kotlin_test_audit.py --group foundation-clreq-core-font --paths` 锁定：35 个
Kotlin `commonTest` 文件，全部位于 `2fae0df..59fca35`，其中 CLREQ 7、core 15、font 7、linebreak 6。
本节的函数级映射以 Kotlin 当前断言为准；“已有直接覆盖”表示 Rust 已有相同行为的明确测试，不能从名称相近、
fixture 或 coverage 名称推断。

#### T2 映射汇总（2026-09-01）

| 组 | Kotlin 测试函数 | 已有直接覆盖 | 需补 Rust 测试 | 需同步生产语义 | 不适用 |
| --- | ---: | ---: | ---: | ---: | ---: |
| CLREQ | 36 | 26 | 10 | 0 | 0 |
| core | 176 | 44 | 128 | 0 | 4 |
| font | 34 | 16 | 6 | 2 | 10 |
| linebreak | 24 | 18 | 5 | 0 | 1 |
| 合计 | 270 | 104 | 149 | 2 | 15 |

不适用的 15 项分两类：Kotlin 运行时可保留孤立 UTF-16 surrogate，而 Rust `Text` 有意只接受有效 UTF-8；
以及 Kotlin Web/ABI 的 CSS inherited-style、exact-font capability probe。两类均不进入 Rust 核心。

#### CLREQ 函数映射

`BopomofoParserTest` 的 4 项、`ClreqPunctuationGlyphSubstitutorTest` 的 4 项、`KinsokuLevelTest` 的 8 项、
`NumberSymbolCohesionTest` 的 3 项和 `PunctuationGluePlacementTest` 的 7 项，都已由同名或等价的
`tests/clreq/{bopomofo_parser,clreq_punctuation_glyph_substitutor,kinsoku_level,number_symbol_cohesion,
punctuation_glue_placement}_test.rs` 直接覆盖。

需直译为 Rust 回归的函数是：

- `ClreqPolicyTailCoverageTest` 的
	`forbiddenAtLineStartCoversEveryPunctuationClass`、`forbiddenAtLineEndCoversOpeningSolidusAndOther`、
	`kinsokuRuleAllowsClustersWithoutDisplayText`、`bopomofoParserCoversEveryToneArm`；
- `ClreqProfileCoverageTest` 的
	`testBopomofoModelsAndParser`、`testClreqProfileAndResolver`、
	`testClreqPunctuationPoliciesAndClassification`、`testForcedHalfWidthAndPolicyFor`、
	`testForbiddenAtLineStartAndEnd`、`testPunctuationAdvanceAndSubstitutor`。

这 10 项均已有 Rust 生产路径，只缺 Kotlin 矩阵的回归测试。最后一项中的孤立 surrogate 子例不迁移，
其余有效 Unicode 输入保留。

#### core 函数映射

现有直接覆盖包括 `CoreBoundaryTest` 的 boundary bias、有效 supplementary scalar、Hangul、RI 与 ZWJ 分支；
`EastAsianSpacingTest` 的 5 项；`UnicodeScriptEvidenceTest`、`UnicodeWordCharacterTest`、
`LinkAddressDisplayTest`、`SourceInteractionBoundariesCoverageTest`、`TextRangeTest` 的全部有效 UTF-8 行为；以及
`LayoutQueriesTest` 的 clipboard projection、autospace occupied box、italic ink、source range、outer glue、
emoji/combining selection 和 inline object selection 子集。对应 Rust 文件是
`tests/core/{source_interaction_boundaries_coverage,east_asian_spacing,unicode_script_evidence,
unicode_word_character,link_address_display,text_range,layout_queries}_test.rs`。

以下文件中除列出的例外外，所有 Kotlin `@Test` 都需按函数边界补 Rust 测试；Rust 生产实现已存在：

| Kotlin 文件 | 需补测试的函数范围 | 已有或不适用例外 |
| --- | --- | --- |
| `CoreBoundaryTest` | 空文本 boundary 与两个 selection offset 函数 | 其余 10 项已有；孤立 surrogate 1 项不适用 |
| `CoreLayoutQueriesGapsTest` | 全部 19 项 | 无 |
| `CoreUnitsGeometryTest` | 除 `textRangeRejectsNegativeStart` 外的 13 项 | `textRangeRejectsNegativeStart` 已有 |
| `EastAsianSpacingCoverageTest` | `testUnicodeWordCharacter`、`testUnicodeScriptEvidence`、`testEastAsianSpacingDataAndValues` | data-class model 1 项不适用；spacing 行为 1 项已有 |
| `EastAsianSpacingLookupCoverageTest` | `lookupCoversEveryGeneratedValueAndBothMissDirections` | 无 |
| `LayoutQueriesTest` | 除 clipboard、autospace box、italic ink、range split、outer trim、emoji/combining selection、inline object selection 外的 19 项 | 列出的 7 项已有 |
| `LayoutQueriesResidualCoverageTest` | 除 `copyProjectionAppendsFullySelectedAnnotationsOnly` 外的 63 项 | copy projection 1 项已有 |
| `TextModelCoverageTest` | 全部 6 项 | 无 |
| `UnicodeNumberTest` | `numbersAreMembersAcrossScriptsAndNonScalarsAreRejected` | 无 |

`SourceInteractionBoundariesCoverageTest` 与 `CoreBoundaryTest` 中的 4 个孤立-surrogate 断言不适用；
有效 supplementary scalar 和所有可表示的 grapheme/selection 行为保留测试。T2 的 core 迁移应按文件拆分，
不能用单个宽泛 integration test 覆盖 128 个函数。

#### font 函数映射与生产差异

`CjkFontRoleClassifierTest` 的 11 项、`ScriptAwareFontMetricsNormalizerTest` 的 3 项和
`UsesLatinFaceTest` 的 2 项已由同名 Rust 测试直接覆盖。`CjkDashCapabilityPolicyTest` 的 5 项和
`InlineShapingStylePolicyTest` 的 5 项是 Kotlin Web host policy，不适用于 Rust core。

6 个仅需测试迁移的 Kotlin 函数为：

- `FontPolicyCoverageTest.testFontRequestAndRoles`；
- `FontPolicyCoverageTest.testPreferCjkForAmbiguousPunctuationResolver`；
- `FontPolicyCoverageTest.testFontEnumsAndModels`；
- `FontPolicyCoverageTest.testFontMetricsRequestAndResolvers`；
- `FontPolicyCoverageTest.testScriptAwareFontMetricsNormalizerBranches`；
- `FontRoleTailCoverageTest.bmpMathAndCurrencySymbolsResolveToSymbolRole`。

以下两项是已确认的生产语义差异，先同步实现再迁移相应有效 Unicode 回归：

| Kotlin 函数与当前规则 | Rust 当前规则 | T2 处理 |
| --- | --- | --- |
| `FontPolicyCoverageTest.testCjkFontRoleClassifierAllRanges`：CJK 固定区段止于 U+3134F，U+32000 为 `Unknown` | ICU `Script::Han` 将 U+32000 判为 `CjkText` | Rust classifier 使用 Kotlin 当前的固定码点区段 |
| `FontRoleTailCoverageTest.supplementarySymbolIsUnknownBecauseItHasNoBmpCategory`：U+1D400 为 `Unknown` | ICU GeneralCategory 将 U+1D400 判为 `Symbol` | Rust symbol 分类限制为 Kotlin 的 BMP `CharCategory` 语义 |

这两项是可观察 font-role 行为，按 Kotlin 当前实现翻译；它们不能由 ICU 更新或个人偏好替代。Kotlin 的
孤立 surrogate 子例仍不适用。

#### linebreak 函数映射

`EnglishHyphenationTest` 的 3 项、`LiangHyphenatorTest` 的 6 项、`MandatoryBreakTest` 的 5 项和
`UnicodePunctuationLineBreakTest` 的 2 项，已由同名 Rust 测试直接覆盖。`LineBreakCoverageTest` 的
`testBundledHyphenationResource`、`testMandatoryBreakAndZeroWidthSpaceCodePoints` 亦已有等价覆盖；
`testLineBreakModelsAndEnums` 是 Kotlin data-class/enum reflection，不适用。

需直译的 5 项为：

- `LineBreakCoverageTest.testSimpleCharacterLineBreakAnalyzer`；
- `LineBreakCoverageTest.testHyphenationComponents`；
- `LineBreakCoverageTest.testUnicodePunctuationLineBreak`；
- `UnicodePunctuationLineBreakCoverageTest.lookupClassesCoverTheUaxTailorablePunctuationClasses`；
- `UnicodePunctuationLineBreakCoverageTest.nonScalarCodePointsAreRejected`。

它们均是已有 Rust linebreak 生产逻辑的边界回归；最后一项按 Rust 的拒绝/panic 机制断言，
不伪造 Kotlin 的 `null` 返回类型。

#### T2 迁移顺序

先修改 font classifier 的两项已确认语义差异，并以精确 regression 固定；然后依 Kotlin 文件顺序迁移
CLREQ、linebreak、font model，最后按 `CoreUnitsGeometry`、`TextModel`、`CoreLayoutQueriesGaps`、
`LayoutQueries`、`LayoutQueriesResidualCoverage` 的依赖关系补 core 测试。每一批只加入对应 Kotlin 函数，
不在 T2 重构 layout query 或 Unicode 数据结构。

#### T2 实施与验证（2026-09-01）

本项的 149 个“需补 Rust 测试”函数均已按 Kotlin 函数边界迁移；没有将名称相近的 fixture、coverage
文件或宽泛循环算作直接对应。测试文件按 Kotlin 行为簇拆分，已有历史测试文件不为本项重命名或重写。

| Kotlin 范围 | Rust 迁移文件 | 实施结果 |
| --- | --- | --- |
| `ClreqPolicyTailCoverageTest`、`ClreqProfileCoverageTest` | `tests/clreq/clreq_policy_tail_coverage_test.rs`、`clreq_profile_coverage_test.rs` | 10 个函数级回归。 |
| `LineBreakCoverageTest`、`UnicodePunctuationLineBreakCoverageTest` | `tests/linebreak/{mandatory_break,liang_hyphenator,unicode_punctuation_line_break}_test.rs` | 5 个函数级回归；非 scalar 输入按 Rust panic 拒绝路径断言。 |
| `FontPolicyCoverageTest`、`FontRoleTailCoverageTest` | `tests/font/{font_policy_coverage_test,font_role_tail_coverage_test}.rs` | 6 个函数级回归。 |
| `CoreBoundaryTest`、`CoreUnitsGeometryTest`、East Asian spacing、`UnicodeNumberTest`、`TextModelCoverageTest` | `tests/core/{core_boundary_test,core_units_geometry_test,east_asian_spacing_coverage_test,east_asian_spacing_lookup_coverage_test,unicode_number_test,text_model_coverage_test}.rs` | 27 个函数级回归。 |
| `CoreLayoutQueriesGapsTest`、`LayoutQueriesTest`、`LayoutQueriesResidualCoverageTest` | `tests/core/{core_layout_queries_gaps_test,layout_queries_t2_coverage_test,layout_queries_residual_geometry_test,layout_queries_residual_rich_text_test,layout_queries_residual_selection_test}.rs`，以及仅供这些模块构造同一手工 `LayoutResult` 的 `layout_queries_test_support.rs` | 101 个函数级回归：19、19、63。 |

font 的两项已确认生产语义差异已在 `src/font/font_policy.rs` 直接按 Kotlin 当前规则翻译：CJK 固定区段止于
U+3134F，使 U+32000 分类为 `Unknown`；symbol 分类先排除 supplementary scalar，保留 Kotlin BMP
`CharCategory` 语义，使 U+1D400 分类为 `Unknown`。测试文件为
`tests/font/cjk_font_role_classifier_test.rs`。

Kotlin 的 15 个不适用函数维持原处置：Rust `Text` 不表示孤立 UTF-16 surrogate，Kotlin Web/ABI 的 CSS
inherited-style 与 exact-font capability probe 也不属于 Rust core。有效 supplementary scalar、grapheme、
selection 与 Unicode number 行为均保留了 Rust 回归。

实际验证结果：

- `cargo test --manifest-path /home/icemic/workspace/tiqian-rs/Cargo.toml --test tiqian clreq_`：15 passed、0 failed。
- `cargo test --manifest-path /home/icemic/workspace/tiqian-rs/Cargo.toml --test tiqian linebreak::`：21 passed、0 failed。
- `cargo test --manifest-path /home/icemic/workspace/tiqian-rs/Cargo.toml --test tiqian font::`：25 passed、0 failed。
- `cargo test --manifest-path /home/icemic/workspace/tiqian-rs/Cargo.toml --test tiqian core::`：159 passed、0 failed。
- `cargo test --manifest-path /home/icemic/workspace/tiqian-rs/Cargo.toml`：458 passed、0 failed；doc-tests 为 0 passed、0 failed。

所有命令仅报告既有 `src/layout/paragraph_dp_line_breaker.rs` 的 `gap_boundaries` dead-code warning；完整
测试输出另报告 example 的既有 dead-code warning。本项没有修改这些无关代码。

### T3：标点 geometry 与行内布局

#### T3 实施前映射（2026-09-01）

T3 的输入已按 `@Test` 紧邻 `fun` 的方式重新提取，不把 `flushTestTrace`、局部 helper 或未注解函数计入。
范围为 24 个 Kotlin `commonTest` 文件、235 个独立测试函数：annotation geometry 11、ASCII point-mark
kinsoku 16、attached inline boundary 6、autospace 13、baseline 4、bilingual emphasis 1、Bopomofo 5、inline
box 2、inline object 13、interpunct 2、opening bracket 1、punctuation atom/HALT 11、body floor 1、punctuation
branch arms 21、engine geometry 20、ledger 16、stage 21、model 20、spacing rule 9、PushIn capacity 5、R3 tail
7、ruby 7、spacing and line geometry 21、verbatim autospace 2。

每个 Kotlin `@Test fun` 是一个映射单位，必须由一个独立 Rust `#[test]` 覆盖。已有 Rust 测试只有在其输入与
断言完整覆盖同一 Kotlin 函数时才计为已有对应物；相似 fixture、文件名、循环和将两个 Kotlin 行为合入一个
Rust test 均不计为完成。现有合并测试在本项中拆分为独立函数。

生产代码对照最初未发现差异；实施过程中确认两项实际 Kotlin→Rust 分叉，均经用户批准后按上游修复：

- `punctuation_geometry_stage.rs` 的 attached ASCII point-mark 判断要求 preceding cluster 的 source text 非空，
	防止 source-empty internal IR 被错误视作可附着的 ASCII mark。
- `line_repair.rs` 的 partial PushIn 在暴露新的当前行后继续重检同一 line index，保持 Kotlin repair loop 的
	`continue` 语义，不跳过刚暴露的 forbidden line start。

其余 punctuation atom、HALT/ink、geometry ledger、autospace、ASCII point-mark kinsoku、attached inline
virtual boundary、inline box/object、ruby、Bopomofo、annotation geometry、baseline、emphasis 与 line geometry
均已有对应 production path。Kotlin `PunctuationGeometryLedger` 的 `clusterIndexRangeFor` 二分查询只是已登记的
P3 性能工作，保持不混入 T3。

用户已确认：Kotlin 直接构造的空 source、非空 display `Cluster` 仅作为 Rust 内部 IR 的防御性行为测试，
在对应 `src/layout` 模块的 `#[cfg(test)]` 中一函数一测试；不扩大公开 API，也不把异常 IR 声明为正常
`source -> shaping` 输入约定。Kotlin 的对象身份断言翻译为 Rust 值相等与空 decision 断言；Kotlin 的
`IllegalArgumentException` 防御断言翻译为 Rust `#[should_panic]`，不为内部 pipeline API 改为 `Result`。

实施分为四个不重叠的测试簇：

| Kotlin 输入簇 | 函数数 | Rust 测试文件 |
| --- | ---: | --- |
| punctuation model、HALT、spacing、engine、interpunct、opening/body floor | 64 | `tests/layout/punctuation_*_test.rs` 与新 `punctuation_model_coverage_test.rs` |
| punctuation ledger、stage、branch arms | 58 | 新 `punctuation_geometry_{ledger,stage,branch_arms}_coverage_test.rs`，以及两个 production-module unit test |
| ASCII kinsoku、attached inline、autospace、inline box/object | 50 | 既有五个 `tests/layout/*_test.rs` |
| annotation、baseline、emphasis、Bopomofo、PushIn、R3、ruby、spacing、verbatim | 63 | 既有 layout tests 与新 annotation/R3 coverage tests |

本项不修改 `tests/layout/mod.rs` 以外的无关模块；新增 integration test 的注册在实现文件齐备后统一完成。测试
名称使用 Rust snake_case，并在同一函数内保留 Kotlin 原函数本来就有的 breaker、width 或 corpus 循环。

#### T3 实施与验证（2026-09-01）

函数级复审确认 24 个 Kotlin 文件中的 235 个 `@Test fun` 均已映射：234 个由独立 Rust `#[test]` 覆盖；
`PunctuationGeometryLedgerCoverageTest.geometryWithoutBudgetFallsBackToBodyWidth` 仍是获批准的 module-local
internal-IR test，位于 `src/layout/punctuation_geometry_ledger.rs`，因为该异常 ledger 输入不是公开
`source -> shaping` 输入约定。没有缺失或语义歧义的函数。

本项新增或拆分了 punctuation model、ledger/stage/branch arms、autospace、ASCII kinsoku、attached inline、
inline object、ruby、Bopomofo、annotation geometry、R3 tail、spacing/line geometry 与 justification 的函数级回归。
`AnnotationGeometryStageCoverageTest` 的 11 个 Kotlin 函数全部单独落在
`tests/layout/annotation_geometry_stage_coverage_test.rs`；其中两个直接构造 `AnnotationGeometryRequest` 的函数
保持 direct-stage 形式，不用公共 layout fixture 近似替代。

实际验证结果：

- 新增 spacing/line geometry 的 5 个唯一函数分别通过；既有模块定向运行显示 11 passed、0 failed。
- 新增 annotation public/direct-stage 映射定向运行通过；两个 direct-stage 函数各为 1 passed、0 failed。
- 函数级审计结果：235/235，234 个独立 integration test 加 1 个获批准的 module-local internal-IR test。
- `cargo test --manifest-path /home/icemic/workspace/tiqian-rs/Cargo.toml`：643 passed、0 failed；doc-tests 为
	0 passed、0 failed。
- `git diff --check`：通过。

以上 Cargo 命令仅报告既有 `src/layout/paragraph_dp_line_breaker.rs` 的 `gap_boundaries` dead-code warning；本项未修改该无关代码。

### T4：shaping、role/cache、quote/dash 与 Unicode/emoji 边界

#### T4 实施前映射（2026-09-01）

按 `tools/list_kotlin_test_audit.py --group shaping-and-unicode-boundaries --paths` 重新锁定输入：22 个资产，
其中 commonTest 21 个、linuxX64Test 1 个。只统计 `@Test fun`，不计 helper、`@AfterTest` 或局部函数，得到
451 个 Kotlin 测试函数：layout 430、common shaping 13、Kotlin/Native backend 8。

函数级审计不把同名文件、类似 fixture、循环输入或一个 Rust 测试合并的多个 Kotlin 场景计为映射。P0-1/P0-2
已经实施 contextual dash/ellipsis、parenthetical pair、数字引号和 prime 的生产行为，但不能替代本项的一函数一
测试映射。对有效 UTF-8 的 Rust public pipeline，尚未发现新的已证实生产语义分叉；因此先翻译纯 contract 与
shaping 测试，不修改生产逻辑。

下列 Kotlin 函数不适用，必须保留理由而非强行模拟：孤立 UTF-16 surrogate 输入不能由 Rust `Text`（有效 UTF-8）
构造；有效 supplementary scalar 仍须迁移。`NativeFontBackendTest` 的 8 个函数验证 Kotlin/Native revision-2
vtable、packed buffer 与 callback retry ABI；独立 Rust core 没有该 ABI producer/consumer，不能把 Kotlin host
协议引入本仓库。其余函数继续逐项映射。

#### T4 首批 shaping contract 映射与验证（2026-09-01）

本批只翻译 6 个没有 production 差异的 Kotlin 函数，均为独立 Rust `#[test]`：

| Kotlin 测试函数 | Rust 对应测试 | 结论 |
| --- | --- | --- |
| `ReplayableFontBackendCoverageTest.fontFaceIdRejectsBlankAndKeepsValue` | `tests/shaping/replayable_font_backend_coverage_test.rs::font_face_id_rejects_blank_and_keeps_value` | 已翻译；Kotlin 参数检查转为 Rust panic 断言。 |
| `ReplayableFontBackendCoverageTest.faceDescriptorDefaultsAreStable` | `tests/shaping/replayable_font_backend_coverage_test.rs::face_descriptor_defaults_are_stable` | 已翻译；Kotlin `copy` 改为 Rust builder。 |
| `ReplayableFontBackendCoverageTest.faceRequestRejectsNonPositiveAndNonFiniteFontSize` | `tests/shaping/replayable_font_backend_coverage_test.rs::face_request_rejects_non_positive_and_non_finite_font_size` | 已翻译；Kotlin 参数检查转为 Rust panic 断言。 |
| `ReplayableFontBackendCoverageTest.capabilityReportReplayFlagRequiresFacesAndNoMissingFaceIssue` | `tests/shaping/replayable_font_backend_coverage_test.rs::capability_report_replay_flag_requires_faces_and_no_missing_face_issue` | 已翻译。 |
| `ReplayableFontBackendCoverageTest.catalogContractResolvesByRequest` | `tests/shaping/replayable_font_backend_coverage_test.rs::catalog_contract_resolves_by_request` | 已翻译；Kotlin anonymous object 改为测试内 Rust trait 实现。 |
| `TextShaperCoverageTest.shapingInputWithFeaturesAndConstants` | `tests/shaping/explainable_stub_text_shaper_test.rs::shaping_input_with_features_and_constants` | 已翻译；保留 OpenType feature、stub decision 与 capability constant 断言。 |

实际验证：

- `cargo test --manifest-path /home/icemic/workspace/tiqian-rs/Cargo.toml --test tiqian shaping::replayable_font_backend_coverage_test -- --nocapture`：5 passed、0 failed。
- `cargo test --manifest-path /home/icemic/workspace/tiqian-rs/Cargo.toml --test tiqian shaping::explainable_stub_text_shaper_test::shaping_input_with_features_and_constants`：1 passed、0 failed。

两条命令都仅报告既有 `src/layout/paragraph_dp_line_breaker.rs` 的 `gap_boundaries` dead-code warning。T4 仍在进行中；
下一批从 `TextShaperCoverageTest` 剩余可迁移的有效 UTF-8 函数开始，再进入 replayable catalog、role resolution
和 Unicode punctuation boundary 的函数级映射。

#### T4 第二、三批 shaping 与 metrics 映射（2026-09-01）

`TextShaperCoverageTest` 的其余四个函数已分别迁移到
`tests/shaping/explainable_stub_text_shaper_test.rs`：`covers_all_shaping_source_enum_entries`、
`unimplemented_text_shaper_throws_on_shape`、`explainable_stub_nominal_advance_branches` 与
`surrogate_pair_handling_in_code_point_count`。最后一项保留 Kotlin 的一个、两个有效 surrogate pair 对应的
Rust supplementary scalar 输入（`😀`、`😀𠀋`）；Kotlin 特有的两个孤立 surrogate 子场景不适用，不能用替代
字符或人为 UTF-16 容器伪造。

`FontInstanceMetricsRequestTest` 的原 Rust 合并测试已拆为两个独立函数，消除一 test 覆盖两个 Kotlin 函数的
情况；其 3 个 Kotlin 函数现在逐项对应：

| Kotlin 测试函数 | Rust 对应测试 | 结论 |
| --- | --- | --- |
| `FontInstanceMetricsRequestTest.perSpanWeightAndItalicReachTheMetricsResolver` | `tests/layout/font_instance_metrics_request_test.rs::per_span_weight_and_italic_reach_metrics_resolver` | 已有独立映射。 |
| `FontInstanceMetricsRequestTest.faceSelectionUsesTheDisplayTextThatWasActuallyShaped` | `tests/layout/font_instance_metrics_request_test.rs::face_selection_uses_the_display_text_that_was_actually_shaped` | 已拆分翻译。 |
| `FontInstanceMetricsRequestTest.rubyMetricsUseTheSameItalicInstanceAsRubyShaping` | `tests/layout/font_instance_metrics_request_test.rs::ruby_metrics_use_the_same_italic_instance_as_ruby_shaping` | 已拆分翻译。 |

实际验证：

- `cargo test --manifest-path /home/icemic/workspace/tiqian-rs/Cargo.toml --test tiqian shaping::explainable_stub_text_shaper_test -- --nocapture`：8 passed、0 failed。
- `cargo test --manifest-path /home/icemic/workspace/tiqian-rs/Cargo.toml --test tiqian layout::font_instance_metrics_request_test`：3 passed、0 failed。
- 本轮局部 `git diff --check`：通过。

上述 Cargo 命令均只报告既有 `src/layout/paragraph_dp_line_breaker.rs` 的 `gap_boundaries` dead-code warning。T4
剩余重点仍是 paragraph shaping、cache、role/quote/dash 和 Unicode punctuation 的函数级映射。

#### T4 第四批 paragraph shaping helper 映射（2026-09-01）

`ParagraphShapingStageCoverageTest` 已开始逐函数迁移；本批两个 direct-stage helper 函数位于新建的
`tests/layout/paragraph_shaping_stage_coverage_test.rs`，不以 layout fixture 近似替代：

| Kotlin 测试函数 | Rust 对应测试 | 结论 |
| --- | --- | --- |
| `ParagraphShapingStageCoverageTest.mapToClusterRangeWithZeroAndPositiveAdvance` | `layout::paragraph_shaping_stage_coverage_test::map_to_cluster_range_with_zero_and_positive_advance` | 已翻译；零 glyph advance 时均分 cluster advance，正 advance 保持不变。 |
| `ParagraphShapingStageCoverageTest.clusterPredicatesAndCurlyQuoteFeatures` | `layout::paragraph_shaping_stage_coverage_test::cluster_predicates_and_curly_quote_features` | 已翻译；保留 mandatory、zero-width、inline-object predicate 和 curly quote layout 路径。 |

实际验证：`cargo test --manifest-path /home/icemic/workspace/tiqian-rs/Cargo.toml --test tiqian
layout::paragraph_shaping_stage_coverage_test` 为 2 passed、0 failed。命令仅报告既有
`src/layout/paragraph_dp_line_breaker.rs` 的 `gap_boundaries` dead-code warning。

#### T4 第五批 paragraph shaping 与 opaque hard-cut 修复（2026-09-01）

下列三个 Kotlin 函数已逐项翻译到 `tests/layout/paragraph_shaping_stage_coverage_test.rs`：

| Kotlin 测试函数 | Rust 对应测试 | 结论 |
| --- | --- | --- |
| `ParagraphShapingStageCoverageTest.hyphenAdvanceFallbackWhenShaperReturnsEmptyClusters` | `layout::paragraph_shaping_stage_coverage_test::hyphen_advance_fallback_when_shaper_returns_empty_clusters` | 已翻译；Rust `ShapingInput` 保存全文和当前 range，测试按 range 读取 `-` segment。 |
| `ParagraphShapingStageCoverageTest.dashSubstitutionRollbackAndCoverageBranches` | `layout::paragraph_shaping_stage_coverage_test::dash_substitution_rollback_and_coverage_branches` | 已翻译；保留 deficient/sufficient ink、unverified/missing glyph rollback、zero/multiple/null-bounds glyph 分支。 |
| `ParagraphShapingStageCoverageTest.latinSegmentationAndCutsBranches` | `layout::paragraph_shaping_stage_coverage_test::latin_segmentation_and_cuts_branches` | 已翻译；保留 Kotlin 的 custom hyphenator、五组 token 和 measure。 |

最后一项首次运行暴露实际生产分叉：Rust `opaque_hard_cuts` 直接按 `clean` 输入顺序建立相邻 range，在 Kotlin 原始
token 中会产生倒序 pair，继而 panic。Kotlin `latinOpaqueHardCuts` 先对相对 bounds 执行 `distinct().sorted()`。
用户确认后，Rust 在 `src/layout/paragraph_shaping_stage.rs` 对 bounds 执行同样的 `sort()` 和 `dedup()`，再遍历相邻
pair；这不改变 cut 集合、优先级或公开 API，只恢复上游保证的有效 source range 顺序。

实际验证：

- `cargo test --manifest-path /home/icemic/workspace/tiqian-rs/Cargo.toml --test tiqian layout::paragraph_shaping_stage_coverage_test::hyphen_advance_fallback_when_shaper_returns_empty_clusters`：1 passed、0 failed。
- 修复前 `latin_segmentation_and_cuts_branches` 在 `opaque_hard_cuts` 的倒序 range 处 panic；修复后该唯一函数为 1 passed、0 failed。
- `cargo test --manifest-path /home/icemic/workspace/tiqian-rs/Cargo.toml --test tiqian layout::paragraph_shaping_stage_coverage_test`：5 passed、0 failed。

上述 Cargo 命令均只报告既有 `src/layout/paragraph_dp_line_breaker.rs` 的 `gap_boundaries` dead-code warning。

#### T4 第六批 paragraph shaping technical 分支映射（2026-09-01）

下列 Kotlin 函数已逐项翻译到 `tests/layout/paragraph_shaping_stage_coverage_test.rs`：

| Kotlin 测试函数 | Rust 对应测试 | 结论 |
| --- | --- | --- |
| `ParagraphShapingStageCoverageTest.progressiveTechnicalSpanBreaksAndTiers` | `layout::paragraph_shaping_stage_coverage_test::progressive_technical_span_breaks_and_tiers` | 已翻译；直接调用 annotation/prep 两阶段，保留嵌套 technical spans、三种单 tier 与 Structural+Syllable 组合。 |
| `ParagraphShapingStageCoverageTest.multiClusterShaperForWordCutsAndOpaqueHardCuts` | `layout::paragraph_shaping_stage_coverage_test::multi_cluster_shaper_for_word_cuts_and_opaque_hard_cuts` | 已翻译；保留交替 two-cluster shaper 与 `[1, 2, 3]` hyphenation offsets。 |
| `ParagraphShapingStageCoverageTest.latinSeparatorCutsAndSolidusBranches` | `layout::paragraph_shaping_stage_coverage_test::latin_separator_cuts_and_solidus_branches` | 已翻译；保留 URL、solidus、opaque separator 原始文本及 500px/1px measure。 |

实际验证：三个唯一函数均为 1 passed、0 failed。所有命令仅报告既有
`src/layout/paragraph_dp_line_breaker.rs` 的 `gap_boundaries` dead-code warning。

#### T4 第七批 paragraph shaping 完整函数映射（2026-09-01）

`ParagraphShapingStageCoverageTest` 的其余 6 个 Kotlin 函数已逐项翻译到
`tests/layout/paragraph_shaping_stage_coverage_test.rs`；该文件现有 14/14 个 Kotlin `@Test fun` 的独立 Rust
`#[test]`，没有以循环或公共 layout fixture 替代 direct-stage 调用。

| Kotlin 测试函数 | Rust 对应测试 | 结论 |
| --- | --- | --- |
| `ParagraphShapingStageCoverageTest.latinWordCutsLoHiAndEmptyBranches` | `layout::paragraph_shaping_stage_coverage_test::latin_word_cuts_lo_hi_and_empty_branches` | 已翻译；保留四个 word 的 hyphen offsets 与 `em` 双 cluster branch。 |
| `ParagraphShapingStageCoverageTest.directShapeParagraphEdgeCases` | `layout::paragraph_shaping_stage_coverage_test::direct_shape_paragraph_edge_cases` | 已翻译；保留 custom hyphenator、empty-cluster shaper、Latin/CJK/space 三次直接 stage 调用。 |
| `ParagraphShapingStageCoverageTest.progressiveTechnicalTierPriorityAndFalseBranches` | `layout::paragraph_shaping_stage_coverage_test::progressive_technical_tier_priority_and_false_branches` | 已翻译；保留重叠 range、空 range、rejected Structural/Syllable tiers。 |
| `ParagraphShapingStageCoverageTest.latinSeparatorCutsExhaustiveBranches` | `layout::paragraph_shaping_stage_coverage_test::latin_separator_cuts_exhaustive_branches` | 已翻译；保留 bibliographic locator、URL、solidus、camel、opaque token 和两种 measure。 |
| `ParagraphShapingStageCoverageTest.progressiveTierLoopRevisitsOffsetsWithLowerPriorityTiers` | `layout::paragraph_shaping_stage_coverage_test::progressive_tier_loop_revisits_offsets_with_lower_priority_tiers` | 已翻译；保留重叠 segment 的 priority overwrite。 |
| `ParagraphShapingStageCoverageTest.latinSeparatorTokensCoverUrlLeadingSlashAndDashLocators` | `layout::paragraph_shaping_stage_coverage_test::latin_separator_tokens_cover_url_leading_slash_and_dash_locators` | 已翻译；每个 Kotlin token/measure 组合保留在同一独立 test 内。 |

实际验证：上述后六个新增函数分别为 1 passed、0 failed；
`cargo test --manifest-path /home/icemic/workspace/tiqian-rs/Cargo.toml --test tiqian
layout::paragraph_shaping_stage_coverage_test` 为 14 passed、0 failed。所有命令仅报告既有
`src/layout/paragraph_dp_line_breaker.rs` 的 `gap_boundaries` dead-code warning。

#### T4 第八批 width-independent annotation cache 完整函数映射（2026-09-01）

`WidthIndependentAnnotationCacheCoverageTest` 的 15 个 Kotlin `@Test fun` 已逐项翻译到新建的
`tests/layout/width_independent_annotation_cache_coverage_test.rs`，并由 `tests/layout/mod.rs` 注册。所有 direct
annotation/prep 函数均调用 `prepare_width_independent_annotation` 与 `build_paragraph_layout_prep`，没有用公共
`layout()` 近似替代。

| Kotlin 测试函数 | Rust 对应测试 | 结论 |
| --- | --- | --- |
| `lruCacheUpdateExistingKeyAndClear` | `lru_cache_update_existing_key_and_clear` | 已翻译；复用 engine preparation 生成的 annotation 值，覆盖 update、evict、clear。 |
| `containingItemsAndFirstContainedItemBranches` | `containing_items_and_first_contained_item_branches` | 已翻译；固定两种单调包含 join 的完整 `Option` 结果。 |
| `prepareWidthIndependentAnnotationBranches` | `prepare_width_independent_annotation_branches` | 已翻译；保留 span、ruby、inline box/object 与 annotation closure 断言。 |
| `lineLengthGridBodyAlignmentBranches` | `line_length_grid_body_alignment_branches` | 已翻译；直接固定 Start/Center/End 的 body offset。 |
| `dynamicShapingTriggersAndEmphasisItalic` | `dynamic_shaping_triggers_and_emphasis_italic` | 已翻译；保留 fast path、rejected tier 与 over-measure 三条触发路径。 |
| `conflictingOpenTypeFeaturesThrows` | `conflicting_open_type_features_throws` | 已翻译；Rust panic 断言同一 conflict 文本。 |
| `adjacentInlineObjectBoundariesMergingAndConflicts` | `adjacent_inline_object_boundaries_merging_and_conflicts` | 已翻译；保留 16 个 boundary boolean 组合和 stretch-class conflict panic。 |
| `verbatimRangesAndAutoSpaceDecisions` | `verbatim_ranges_and_auto_space_decisions` | 已翻译；保留 suppressed range 与 Narrow inline box。 |
| `rubySpreadAccumulationAndEdges` | `ruby_spread_accumulation_and_edges` | 已翻译；保留 source boundaries、三个有效及一个无效 ruby range。 |
| `shrinkOpportunitiesCoverAllPunctuationClassesAndSpaces` | `shrink_opportunities_cover_all_punctuation_classes_and_spaces` | 已翻译；保留 Taiwan profile、逐 UTF-16 span、两个 adjustment 开关的四种组合。 |
| `styleAtAndEmphasisItalicAtAndDynamicShapingBranches` | `style_at_and_emphasis_italic_at_and_dynamic_shaping_branches` | 已翻译；保留 font-size closure 边界、rejected tier、宽/窄 measure 与缺 font decision 回退。 |
| `rubySpreadSecondVisitAndZeroFirstCluster` | `ruby_spread_second_visit_and_zero_first_cluster` | 已翻译；保留同 range 两次 ruby visit 和 source boundaries。 |
| `pairedPunctuationWithZeroCapacity` | `paired_punctuation_with_zero_capacity` | 已翻译。 |
| `dynamicShapingEmphasisItalicAtAndZeroPairedCapacityBranches` | `dynamic_shaping_emphasis_italic_at_and_zero_paired_capacity_branches` | 已翻译；清空 segment cache 后保留 rejected tier dynamic path。 |
| `centeredPunctBeforeAttachedReferenceKeepsLeadingGlueOnly` | `centered_punct_before_attached_reference_keeps_leading_glue_only` | 已翻译；保留 narrow ink shaper、UTF-16 attached reference 与 spacing decision 断言。 |

实际验证：最后三个新增函数均为 1 passed、0 failed；
`cargo test --manifest-path /home/icemic/workspace/tiqian-rs/Cargo.toml --test tiqian
layout::width_independent_annotation_cache_coverage_test` 为 15 passed、0 failed。所有命令仅报告既有
`src/layout/paragraph_dp_line_breaker.rs` 的 `gap_boundaries` dead-code warning。

#### T4 第九批 width-independent annotation cache LRU 映射（2026-09-01）

`WidthIndependentAnnotationCacheTest` 的 5 个 Kotlin `@Test fun` 已重新逐函数审计。下列四个 Rust 函数在
本批前已是独立的一一映射：`relayout_at_three_widths_hits_annotation_cache_without_reshaping`、
`cached_and_uncached_engines_produce_identical_layout_results_across_widths`、
`reflow_fuzzing_random_sequence_produces_exact_output` 与
`cache_key_distinguishes_text_style_decoration_ruby_and_inline_box`。本批补齐最后一项：

| Kotlin 测试函数 | Rust 对应测试 | 结论 |
| --- | --- | --- |
| `WidthIndependentAnnotationCacheTest.lruCacheEvictsOldestEntriesWhenCapacityExceeded` | `tests/layout/width_independent_annotation_cache_test.rs::lru_cache_evicts_oldest_entries_when_capacity_exceeded` | 已翻译；保留 Kotlin 在第三次插入前依次读取 key1、key2 的访问顺序，固定 key1 被淘汰。 |

实际验证：`cargo test --manifest-path /home/icemic/workspace/tiqian-rs/Cargo.toml --test tiqian
layout::width_independent_annotation_cache_test::lru_cache_evicts_oldest_entries_when_capacity_exceeded` 为
1 passed、0 failed。命令仅报告既有 `src/layout/paragraph_dp_line_breaker.rs` 的 `gap_boundaries` dead-code warning。

#### T4 第十批 cluster role resolution 完整函数映射（2026-09-01）

`ClusterRoleResolutionCoverageTest` 的 65 个 Kotlin `@Test fun` 已按 Rust 输入域逐项处置：62 个有效 UTF-8
输入均在新建的 `tests/layout/cluster_role_resolution_coverage_test.rs` 中由独立 Rust `#[test]` 覆盖；测试文件
直接调用 `cluster_role_ranges[_with_options]` 或 `require_covered_by`，不通过 paragraph layout 间接替代。

已翻译的函数覆盖 simple/empty/whitespace 文本、CJK/ASCII punctuation、repeatable punctuation、span/emoji shaping
boundaries、CR/LF/CRLF mandatory breaks、zero-width space、inline object、combining mark、variation selector、keycap、
emoji modifier/ZWJ/RGI promotion、supplementary scalar，以及 `requireCoveredBy` 的 contiguous、multi-decision、empty 和
五条 panic invariant。每一 Kotlin 函数均保留一个独立 Rust 函数；同一 Kotlin 函数内部本有的输入组合维持在其对应
test 中。

以下 3 个 Kotlin 函数为不适用：`clusterRoleRangesWithLoneSurrogate`、
`clusterRoleRangesWithLoneSurrogateHighOnly`、`clusterRoleRangesWithSurrogatePairNonLow`。它们以运行时 UTF-16
code unit 构造孤立 high surrogate；Rust `Text` 仅接受有效 UTF-8，不能也不应伪造这一输入。对应的有效
supplementary scalar 与 emoji UTF-16 range 行为已保留 Rust 测试。

实际验证：新函数 `cluster_role_ranges_with_multiple_emoji_shaping_boundaries` 为 1 passed、0 failed；
`cargo test --manifest-path /home/icemic/workspace/tiqian-rs/Cargo.toml --test tiqian
layout::cluster_role_resolution_coverage_test` 为 62 passed、0 failed。所有命令仅报告既有
`src/layout/paragraph_dp_line_breaker.rs` 的 `gap_boundaries` dead-code warning。

#### T4 第十一批 cluster role extender edge 映射（2026-09-01）

`ClusterRoleResolutionSurrogateAndExtenderEdgeTest` 的 11 个 Kotlin `@Test fun` 已逐项处置。9 个有效 UTF-8
输入追加为 `tests/layout/cluster_role_resolution_coverage_test.rs` 的独立函数：astral variation selector 的
in-range/upper-bound、attached ASCII point mark、emoji modifier-base、BMP selector、ZWJ、span boundary 与 inline
object-over-CRLF 分支。每项固定 Kotlin 的 UTF-16 source range、role 或 mandatory-break 结果。

`highSurrogateBeforePrivateUseKeepsTheLoneHalf` 与 `highSurrogateBeforePlainBmpKeepsTheLoneHalf` 依赖孤立 high
surrogate，不能由 Rust 有效 UTF-8 `Text` 构造，登记为不适用；其余 supplementary variation selector 和 emoji
sequence 路径均保留可执行 Rust 断言。

实际验证：`astral_variation_selector_extends_the_run_before_it` 为 1 passed、0 failed；完整
`layout::cluster_role_resolution_coverage_test` 为 71 passed、0 failed。所有命令仅报告既有
`src/layout/paragraph_dp_line_breaker.rs` 的 `gap_boundaries` dead-code warning。

#### T4 第十二批 contextual dash/ellipsis 函数映射（2026-09-01）

`ContextualDashEllipsisRoleResolverCoverageTest`、`ContextualRoleExtensionCoverageTest` 与
`ContextualDashEllipsisClusterCoverageTest` 共含 7 个 Kotlin `@Test fun`。其中 6 个有效 UTF-8 函数均已在
`tests/layout/contextual_dash_ellipsis_role_resolver_test.rs` 保留独立 Rust `#[test]`：parenthetical dash pair 的
only-left、only-right、无 outer-script 三条 resolver 分支；pipeline 外 contextual wrapper；Latin dash run 的段末
cluster；以及 style span 切开的 Latin dash run。

用户确认采用公开 API 同步后，Rust 新增 `with_contextual_dash_ellipsis_roles` 与
`with_contextual_quote_roles`。两个函数分别返回实现 `FontRoleClassifier` 的 contextual classifier enum：没有
decision 时为 `Passthrough` 并保留传入 delegate；有 decision 时为 `Overrides`，只覆盖相应 UTF-16 source
start，其他 range 委托 base classifier。这一 API 保留 Kotlin `withContextual*Roles` 的 pipeline 外调用语义，且
不改变既有 cache 内 quote-aware 再 dash-aware 的装配路径。

`forwardPassWalkerArmsRunBeforeTheClassifierRejectsLoneSurrogates` 为不适用：它依赖三个运行时构造的 malformed
UTF-16 输入，Rust `Text` 只表示有效 UTF-8，不能构造同一 source 输入。有效 contextual dash/ellipsis、role
override 与 cluster 行为仍通过上述六个独立测试覆盖。

实际验证：`contextual_role_extensions_wrap_outside_the_pipeline`、
`style_span_inside_latin_dash_run_splits_the_cluster` 分别为 1 passed、0 failed；完整
`layout::contextual_dash_ellipsis_role_resolver_test` 为 12 passed、0 failed。所有命令仅报告既有
`src/layout/paragraph_dp_line_breaker.rs` 的 `gap_boundaries` dead-code warning。

#### T4 第十三批 Unicode punctuation boundary 首批映射（2026-09-01）

`UnicodePunctuationBoundaryResolverCoverageTest` 共 122 个 Kotlin `@Test fun`，按 direct API 划分为 100 个
`resolveUnicodePunctuationBoundaries`、3 个 `resolveAttachedInlineVirtualBoundaries` 和 19 个
`resolveAttachedInlineInterCharBoundaries`。既有 Rust 的 line-break 与 attached-inline integration tests 输入、断言和
函数边界均不同，严格的一函数一测试计数从 0 开始。

首批 8 个 Kotlin 函数已进入新建并注册的
`tests/layout/unicode_punctuation_boundary_coverage_test.rs`：3 条 attached virtual run 分支、paired quote、
western closing LB13、opening punctuation LB14、ASCII infix LB15d，以及 attached run 跨 Wide/Narrow 的
sino-western virtual boundary。所有函数直接调用对应 resolver，不使用 paragraph layout 近似替代。

审计确认仅 `resolveUnicodePunctuationBoundariesApostropheRightNeighbourUnpairedHighSurrogate` 依赖 malformed
lone high surrogate，后续登记为不适用；文件中其余 16 个 emoji/supplementary 场景都使用有效 Unicode scalar，
仍须迁移。对有效 Unicode scalar 的 Kotlin/Rust resolver 路径尚未发现已确认生产语义差异。

实际验证：`resolve_attached_inline_inter_char_boundaries_sino_western_only` 为 1 passed、0 failed；完整
`layout::unicode_punctuation_boundary_coverage_test` 为 8 passed、0 failed。所有命令仅报告既有
`src/layout/paragraph_dp_line_breaker.rs` 的 `gap_boundaries` dead-code warning。

#### T4 第十四批 Unicode punctuation boundary 基础分支映射（2026-09-01）

`tests/layout/unicode_punctuation_boundary_coverage_test.rs` 新增 21 个独立函数级映射。第一组为 unmatched
closing、CJK closing paired quote、exclamation、initial/unresolved quote、multiple cluster、empty cluster 与 all-CJK
text。第二组为 attached inter-char 的 CJK/CJK、western bracket、CJK body western bracket，以及 cluster/role/edge
alignment 和 attachment alignment 两条 panic 输入一致性检查。第三组为 punctuation after space、mandatory authored boundary、
close/IS class、decimal mark 的 space/non-space/无 trailing digit 分支和 previous-content 空白扫描。

每项保留 Kotlin 的 direct resolver 调用与独立 Rust `#[test]`；Kotlin `require` 的输入对齐失败在 Rust 中用同一
production `assert!` 的 `#[should_panic]` 固定。当前文件累计 29 个独立函数映射，尚未将相似的 line-break
integration tests 计入该 Kotlin coverage 文件。

实际验证：`resolve_attached_inline_inter_char_boundaries_requires_matching_attachment_size`、
`resolve_unicode_punctuation_boundaries_with_decimal_mark_after_space` 分别为 1 passed、0 failed；完整
`layout::unicode_punctuation_boundary_coverage_test` 为 29 passed、0 failed。所有命令仅报告既有
`src/layout/paragraph_dp_line_breaker.rs` 的 `gap_boundaries` dead-code warning。

#### T4 第十五批 Unicode punctuation boundary quote/content 映射（2026-09-01）

为控制单个 test 源文件的长度，新增并注册
`tests/layout/unicode_punctuation_boundary_resolver_coverage_test.rs`。本批 15 个独立 Kotlin 函数分两组迁移：
第一组固定 U+201D/U+201C/U+00AB 的 final/initial/unresolved direction、U+2019 word apostrophe、Latin-1
letter、first/last significant code point；第二组固定 mandatory authored break、next/previous content cluster、
close/exclamation/close-parenthesis class、LB15d reason 与 default line-start 分支。

所有函数直接调用 `resolve_unicode_punctuation_boundaries`，保留 Kotlin 的 source text、UTF-16 cluster range、
role 与 `unbreakable_ranges` 输入结构。当前 `UnicodePunctuationBoundaryResolverCoverageTest` 已有 44 个独立的
Rust function mapping；100 个 main resolver、3 个 attached virtual 和 19 个 attached inter-char 函数仍按原文件
逐项继续，未从既有相似 integration tests 推断已覆盖。

实际验证：`resolve_unicode_punctuation_boundaries_with_latin_word_code_point`、
`resolve_unicode_punctuation_boundaries_with_next_content_cluster` 分别为 1 passed、0 failed；
`layout::unicode_punctuation_boundary_coverage_test` 为 36 passed、0 failed，
`layout::unicode_punctuation_boundary_resolver_coverage_test` 为 8 passed、0 failed。所有命令仅报告既有
`src/layout/paragraph_dp_line_breaker.rs` 的 `gap_boundaries` dead-code warning。

#### T4 第十六批 Unicode punctuation boundary authored/supplementary 映射（2026-09-01）

`unicode_punctuation_boundary_resolver_coverage_test.rs` 增加 8 个独立 Kotlin 函数：next-content authored break、
emoji 的 non-BMP whitespace 路径、两侧 mandatory authored break、纯空白和空白后非空白的 authored-boundary、
previous/next content 为空，以及 U+201C 后跟完整 emoji scalar 的 `codePointAtOrNull` 路径。emoji 一律用 Rust
`😀` 与 UTF-16 `TextRange(1, 3)` 构造，不伪造 surrogate code unit。

实际验证：`resolve_unicode_punctuation_boundaries_with_code_point_at_or_null_surrogate` 为 1 passed、0 failed；
`layout::unicode_punctuation_boundary_resolver_coverage_test` 为 16 passed、0 failed。连同第一 coverage 文件，
该 Kotlin 文件当前为 52 个独立 Rust function mapping；所有命令仅报告既有
`src/layout/paragraph_dp_line_breaker.rs` 的 `gap_boundaries` dead-code warning。

#### T4 第十七批 Unicode punctuation boundary decimal/attached 映射（2026-09-01）

本批新增 11 个独立 Kotlin 函数映射。resolver 侧包括 empty range、first code point length、CR/ZWSP authored
boundary、decimal mark 后同 cluster/下一 cluster digit 与 previous-content authored break。attached inter-char 侧
包括 edges size mismatch panic，以及 narrow trailing、CJK punctuation 和 CJK body 组合下的三条 virtual-boundary
分支。所有函数继续直接调用 resolver；同 cluster 的 `23` 使用 Kotlin 对等的 UTF-16 `TextRange(3, 5)`。

`unicode_punctuation_boundary_coverage_test` 当前为 40 passed、0 failed，
`unicode_punctuation_boundary_resolver_coverage_test` 当前为 23 passed、0 failed；该 Kotlin coverage 文件累计
63 个独立 Rust function mapping。`git diff --check` 通过，追踪文档文风检查为 0 命中，所有 Cargo 命令仅报告
既有 `src/layout/paragraph_dp_line_breaker.rs` 的 `gap_boundaries` dead-code warning。

#### T4 第十八批 Unicode punctuation boundary attached/astral 映射（2026-09-01）

新增并注册 `unicode_punctuation_boundary_attached_inline_coverage_test.rs`，补齐该 Kotlin 文件全部 19 个
`resolveAttachedInlineInterCharBoundaries` 函数的一函数一 Rust test 映射。新文件覆盖 sino-western pair、双
CJK 标点、leading/trailing narrow-only、western bracket-only，以及条件均不成立的路径；不复用输入或函数边界
不同的既有 integration tests。

resolver coverage 文件新增 10 个独立函数：decimal mark 位于 cluster zero、前一个 letter cluster、letter trailing、
space 后无 trailing，以及 empty cluster 的禁止条件；mandatory break 位于 previous cluster 内部；U+2019 位于文本
起点、完整 emoji pair 前后；以及 emoji 作为 cluster 尾部 last significant code point。所有 emoji 使用有效 scalar
和 Kotlin 等价 UTF-16 range，例如 `.😀` 为 `TextRange(2, 5)`，`’😀` 为 `TextRange(0, 3)`。

实际验证：`layout::unicode_punctuation_boundary_attached_inline_coverage_test` 为 9 passed、0 failed，
`layout::unicode_punctuation_boundary_resolver_coverage_test` 为 33 passed、0 failed。该 Kotlin coverage 文件累计
82 个独立 Rust function mapping；所有 Cargo 命令仅报告既有
`src/layout/paragraph_dp_line_breaker.rs` 的 `gap_boundaries` dead-code warning。

#### T4 第十九批 Unicode punctuation boundary opening/supplementary 映射（2026-09-01）

resolver coverage 文件新增 7 个独立 Kotlin 函数映射：全角 opening punctuation、CJK closing line-start、空白的
first code point、mandatory-only authored break、`中”` 的 preceding code point，以及完整 emoji pair 前后的
`codePointBefore` / `codePointAtOrNull` 路径。`WithHasAuthoredBreakMandatoryOnly` 与已有 CR 输入对应不同 Kotlin
函数，仍保留为单独 Rust `#[test]`。

实际验证：`resolve_unicode_punctuation_boundaries_with_code_point_before_surrogate_pair` 为 1 passed、0 failed，
`layout::unicode_punctuation_boundary_resolver_coverage_test` 为 40 passed、0 failed。该 Kotlin coverage 文件累计
89 个独立 Rust function mapping；所有 Cargo 命令仅报告既有
`src/layout/paragraph_dp_line_breaker.rs` 的 `gap_boundaries` dead-code warning。

#### T4 第二十批 Unicode punctuation boundary content helper 映射（2026-09-01）

resolver coverage 文件新增 13 个独立 Kotlin 函数映射。第一组为 decimal mark 的 index-zero、non-whitespace
previous、empty previous，非 authored opening boundary，以及 previous content 的有内容/仅空 cluster 路径。第二组为
next content 返回内容、mandatory break 带 code point/空 code point，以及 BMP 和完整 emoji pair 的 first/before/at
code-point 路径。相同 source 输入但对应不同 Kotlin `@Test fun` 的 emoji helper 保留独立 Rust `#[test]`。

实际验证：`resolve_unicode_punctuation_boundaries_previous_content_cluster_returns_content` 与
`resolve_unicode_punctuation_boundaries_next_content_cluster_returns_content` 分别为 1 passed、0 failed；
`layout::unicode_punctuation_boundary_resolver_coverage_test` 为 53 passed、0 failed。该 Kotlin coverage 文件累计
102 个独立 Rust function mapping；所有 Cargo 命令仅报告既有
`src/layout/paragraph_dp_line_breaker.rs` 的 `gap_boundaries` dead-code warning。

#### T4 第二十一批 Unicode punctuation boundary coverage 收尾（2026-09-01）

新增 helper coverage 文件余下 7 个独立 Kotlin 函数映射：space/no-space infix numeric separator、decimal-mark
following variations、U+2019 与 Latin word 的四个分支、`SurrogateScanningVariations`、emoji pair 的 single
before/at code point，以及空文本 authored-break 路径。`SurrogateScanningVariations` 保留为一个 Rust `#[test]`，在
函数内逐项运行 Kotlin 原有的两组字符串循环和 ZWSP/mandatory-break 分支；emoji offset 用
`encode_utf16().count()` 与显式 UTF-16 range 对齐。

`UnicodePunctuationBoundaryResolverCoverageTest.kt` 的 122 个 Kotlin `@Test fun` 现已逐项分类：121 个有效输入
均为独立 Rust `#[test]`，`resolveUnicodePunctuationBoundariesApostropheRightNeighbourUnpairedHighSurrogate` 为唯一
不适用项。后者通过 Kotlin 运行时构造 lone high surrogate；Rust `Text` 只表示有效 UTF-8，不能表达同一 source
输入。它不是有效 supplementary scalar，完整 emoji pair 的扫描与 range 行为已由本批及前述测试保留。

实际验证：`resolve_unicode_punctuation_boundaries_surrogate_scanning_variations` 为 1 passed、0 failed，
`layout::unicode_punctuation_boundary_helper_coverage_test` 为 19 passed、0 failed。所有 Cargo 命令仅报告既有
`src/layout/paragraph_dp_line_breaker.rs` 的 `gap_boundaries` dead-code warning。

#### T4 第二十二批 contextual quote resolver 函数映射（2026-09-01）

`ContextualQuoteRoleResolverCoverageTest.kt` 的 39 个 Kotlin `@Test fun` 已逐项迁移到
`tests/layout/contextual_quote_role_resolver_coverage_test.rs`。每项直接调用 Kotlin 对应的
`QuotePairAnalyzer.analyze` 加 `classifyQuoteRoles` 路径；Kotlin 显式传入 `emptyList()` 的函数使用单独的
Rust helper，不由自动配对近似替代。范围包括 nested pair、unmatched 引号、whitespace-delimited 引号、
U+2019 两侧 word evidence、locale fallback 与完整 emoji pair 的 UTF-16 扫描。

`ContextualQuoteRoleResolverNestedAndSurrogateTest.kt` 中 9 个有效 UTF-8 函数已逐项迁移到
`contextual_quote_role_resolver_nested_coverage_test.rs`，并直接调用 `ContextualQuoteRoleResolver::resolve()`；
覆盖 neutral enclosing quotation inheritance、tab/space delimiter、mixed content、locale 与 private-use
边界。`leftwardScanFromALowSurrogateWalksEveryBacktrackArm`、`highSurrogateAtTheContentEndHasNoRoomAndThrows`、
`plainFollowerOfAHighSurrogateCountsAsOneUnit` 与 `privateUseFollowerOfAHighSurrogateCountsAsOneUnit` 为不适用：
它们均以 Kotlin runtime UTF-16 code unit 构造孤立 high/low surrogate，Rust `Text` 无法表达同一 source 输入。

实际验证：`layout::contextual_quote_role_resolver_coverage_test` 为 39 passed、0 failed，
`layout::contextual_quote_role_resolver_nested_coverage_test` 为 9 passed、0 failed。所有 Cargo 命令仅报告既有
`src/layout/paragraph_dp_line_breaker.rs` 的 `gap_boundaries` dead-code warning。

#### T4 第二十三批 QuotePairAnalyzer coverage 有效输入映射（2026-09-01）

`QuotePairAnalyzerCoverageTest.kt` 的 27 个 Kotlin `@Test fun` 已逐项审计。其中 20 个有效 UTF-8 输入已迁移到
`tests/layout/quote_pair_analyzer_coverage_test.rs`，每项保留独立 Rust `#[test]`。测试直接调用
`QuotePairAnalyzer::analyze`、`classify_quote_roles`、两个 deprecated classifier overload，或
`QuotePairAwareFontRoleClassifier`；不以 paragraph layout 近似替代 analyzer 路径。覆盖 complete emoji pair 前后
的 UTF-16 index、空 quote stack、single/nested pair、override delegate 和 out-of-range neighbour。

以下 7 个 Kotlin 函数只构造孤立 UTF-16 surrogate，并断言 Kotlin 在 Unicode word-character lookup 抛出
`IllegalArgumentException`，因此不适用：`codePointBeforeWithLowSurrogateAtStart`、
`codePointBeforeWithLowSurrogateAfterNonHighSurrogate`、`codePointAtOrNullWithHighSurrogateAtEnd`、
`codePointAtOrNullWithHighSurrogateFollowedByNonLowSurrogate`、
`codePointBeforeLowInRangeIndexGe2HighNotInRange`、`codePointAtOrNullLoneHighSurrogateAfterQuote` 与
`codePointAtOrNullHighSurrogateAtStringEnd`。Rust `Text` 只接受有效 UTF-8，不能构造等价 source 输入；完整 emoji
pair 的 valid scalar 路径已由上述 20 项覆盖。

实际验证：`cargo test --manifest-path /home/icemic/workspace/tiqian-rs/Cargo.toml --test tiqian
layout::quote_pair_analyzer_coverage_test::single_quote_pair_match` 为 1 passed、0 failed；完整
`layout::quote_pair_analyzer_coverage_test` 为 20 passed、0 failed。两条命令均仅报告既有
`src/layout/paragraph_dp_line_breaker.rs` 的 `gap_boundaries` dead-code warning。

#### T4 第二十四批 QuotePairAnalyzer surrogate adjacency 映射（2026-09-01）

用户确认 mixed-surrogate Kotlin 函数采用 A：每个 Kotlin `@Test fun` 保留一个独立 Rust `#[test]`，翻译其中的
有效 Unicode scalar 子例；同函数中的 malformed UTF-16 子例单独登记为不适用。`
QuotePairAnalyzerSurrogateAdjacencyTest.kt` 的 4 个函数已迁移到
`tests/layout/quote_pair_analyzer_surrogate_adjacency_coverage_test.rs`。其中
`lowQuoteCodePointsTakeTheSwitchDefaultWithoutPairing` 完整翻译；其余三个函数的 Rust 对应物分别保留完整 emoji
pair、private-use BMP、普通 BMP 与文本边界子例。

`apostropheAfterASurrogatePairWalksTheCombineArmBefore` 的 3 个 lone-low-surrogate 子例、
`apostropheBeforeASurrogateWalksBothLowCheckArms` 的 1 个 lone-high-surrogate 子例，以及
`plainAndBoundaryNeighboursWalkTheNonSurrogateArms` 的 3 个 lone low/high surrogate 子例不适用。它们通过 Kotlin
runtime UTF-16 code unit 构造输入并断言 `IllegalArgumentException`；Rust `Text` 不能表达同一非法 source。每个
函数中的 valid scalar 子例仍由相应独立 Rust test 直接调用 `is_non_cjk_in_word_apostrophe`。

实际验证：`layout::quote_pair_analyzer_surrogate_adjacency_coverage_test` 为 4 passed、0 failed；完整
`layout::quote_pair_analyzer_` 前缀为 34 passed、0 failed。两条命令均仅报告既有
`src/layout/paragraph_dp_line_breaker.rs` 的 `gap_boundaries` dead-code warning。

#### T4 第二十五批 existing shaping 与 Emoji 17 映射核验（2026-09-01）

`ExplainableStubTextShaperTest.kt` 的 3 个 Kotlin `@Test fun` 已逐项确认由
`tests/shaping/explainable_stub_text_shaper_test.rs` 中同名 snake_case Rust test 覆盖：单 CJK cluster 的 source/
display/advance/glyph/decision，Latin run 的 nominal glyphs，以及 CLREQ dash substitution 的 source/display/two-em
advance。每项均直接调用 `ExplainableStubTextShaper::shape`。

`UnicodeEmoji17RgiRoleAuditTest.fullyQualifiedEmojiSequencesResolveToOneEmojiRange` 已由
`tests/layout/unicode_emoji17_rgi_role_audit_test.rs::fully_qualified_emoji_sequences_resolve_to_one_emoji_range`
逐项覆盖。两边均断言 Unicode 17.0.0 的 3944 条 fully-qualified code-point sequence 各自解析为一个完整 UTF-16
Emoji range；Rust 从 valid scalar 构造字符串，等价于 Kotlin 对 supplementary scalar 的 surrogate-pair 编码。

#### T4 第二十六批 historical Unicode punctuation boundary 映射（2026-09-01）

`UnicodePunctuationBoundaryTest.kt` 的四个此前未单独映射的 Kotlin `@Test fun` 已迁移到
`tests/layout/unicode_punctuation_boundary_historical_coverage_test.rs`：
`bracket_boundaries_remain_protected_across_western_spaces`、
`unmatched_western_curly_double_quotes_retain_their_direction`、
`unmatched_elision_apostrophe_binds_forward_instead_of_being_guessed_as_a_closer` 与
`western_baseline_survives_clreq_kinsoku_none`。其余四个函数已有直接 Rust 对应测试，和本批的函数边界不同，未将其
重新合并或作为本批覆盖依据。

`bracketBoundariesRemainProtectedAcrossWesternSpaces` 的首次 Rust 失败不是 production 差异：Kotlin helper 为输入设置
`sourceBoundaries = 0..text.length`，而初始 Rust helper 遗漏了该项，导致 shaping 的 Latin cluster 边界不同，出现
`["ABC", "D", "(  ", "EFGH"]`。Rust helper 现使用相同 UTF-16 全偏移集合；同时显式关闭与 Kotlin 相同的
`LineLengthGrid`。没有修改 production resolver、greedy breaker 或 repair。Kotlin 的精确函数已实际运行并通过。

实际验证：Kotlin `:engine:jvmTest --tests
'org.tiqian.layout.UnicodePunctuationBoundaryTest.bracketBoundariesRemainProtectedAcrossWesternSpaces'` 通过；Rust
`cargo test --manifest-path /home/icemic/workspace/tiqian-rs/Cargo.toml --test tiqian
layout::unicode_punctuation_boundary_historical_coverage_test::` 为 4 passed、0 failed。两端 Rust Cargo 命令仅报告既有
`src/layout/paragraph_dp_line_breaker.rs` 的 `gap_boundaries` dead-code warning。

#### T4 第二十七批 contextual dash/ellipsis resolver 完整函数映射（2026-09-01）

`ContextualDashEllipsisRoleResolverTest.kt` 的 13 个及同文件
`ContextualDashEllipsisLayoutTest` 的 4 个 Kotlin `@Test fun`，共 17 项，已分别迁移到
`tests/layout/contextual_dash_ellipsis_role_resolver_coverage_test.rs`。每个 Kotlin 函数均有一个独立 Rust
`#[test]`；resolver 函数直接调用 `ContextualDashEllipsisRoleResolver::resolve`，layout 函数走公开 paragraph
pipeline，不以已有的相似测试或 fixture 代替。

覆盖范围包括 surrounding-script 与 paragraph-language fallback、reason、mandatory break、有效 supplementary
script evidence、2048 neutral-separated run、parenthetical pair 的四种外侧/独立条件，以及 Latin/CJK role-gated
display substitution、pair face 与 standalone Western ellipsis。此前
`contextual_dash_ellipsis_role_resolver_test.rs` 中的合并测试保留为既有回归，不作为这 17 项的函数级映射记录。

实际验证：`cargo test --manifest-path /home/icemic/workspace/tiqian-rs/Cargo.toml --test tiqian
layout::contextual_dash_ellipsis_role_resolver_coverage_test:: -- --nocapture` 为 17 passed、0 failed，仅报告既有
`src/layout/paragraph_dp_line_breaker.rs` 的 `gap_boundaries` dead-code warning。

#### T4 第二十八批 display glyph substitution 基础函数映射（2026-09-01）

`DisplayGlyphSubstitutionEngineTest.kt` 的前五个 Kotlin `@Test fun` 已分别迁移到
`tests/layout/display_glyph_substitution_engine_coverage_test.rs`：
`preserves_source_text_when_using_clreq_recommended_display_glyphs`、
`honors_profile_punctuation_glyph_policy`、`coalesce_set_is_driven_by_profile`、
`uses_two_em_advance_for_recommended_dash_codepoint` 与
`preserves_open_type_features_as_final_glyph_run_boundaries`。每项独立走 Kotlin 对应的 public layout pipeline；最后一项
使用测试内 `TextShaper`，逐 cluster 返回 glyph run 与 feature，固定最终 glyph-run 不能跨 OpenType feature 边界。

实际验证：上述五个 Rust `#[test]` 已逐一运行，均为 1 passed、0 failed。命令仅报告既有
`src/layout/paragraph_dp_line_breaker.rs` 的 `gap_boundaries` dead-code warning。

#### T4 第二十九批 display glyph substitution 完整函数映射（2026-09-01）

`DisplayGlyphSubstitutionEngineTest.kt` 的 18 个 Kotlin `@Test fun` 现已逐项完成映射。此前第 28 批的五项基础
函数位于 `tests/layout/display_glyph_substitution_engine_coverage_test.rs`；本批继续在该文件逐项添加无 ink bounds、
missing glyph、unverified coverage、three dash ink coverage 分支、span font size、justification boundary、default
substitution、ambiguous glyph mapping 和多字符 local ink 的 12 个函数。Kotlin
`stubShaperReportsProfileFallbackWhenInkBoundsAreUnavailable` 已由既有
`tests/layout/display_glyph_substitution_engine_test.rs::stub_shaper_records_named_profile_fallback_without_ink_bounds`
完整覆盖，保留其原有 Rust 测试，不重复实现。

每个 rollback 测试只复现 Kotlin 对应 `TextShaper` 提供的 evidence：`.notdef`、
`UnverifiedDisplaySubstitutionCoverage` 或 glyph ink bounds；没有修改 substitution、geometry、breaker 或
justification production 路径。最后的 justification regression 保留 Kotlin 的 Lookahead breaker、关闭 grid、
13..30 cells 的同一 width 搜索和 `CjkInterChar` allocation 排除断言。

实际验证：新增 coverage 模块注册 17 个独立 Rust `#[test]`，完整运行结果为 17 passed、0 failed；最后的
justification 函数也单独通过。命令仅报告既有 `src/layout/paragraph_dp_line_breaker.rs` 的
`gap_boundaries` dead-code warning。

#### T4 第三十批 QuotePairAnalyzerTest 完整函数映射（2026-09-01）

`QuotePairAnalyzerTest.kt` 的 31 个 Kotlin `@Test fun` 已逐项迁移至
`tests/layout/quote_pair_analyzer_historical_coverage_test.rs`。测试直接调用
`QuotePairAnalyzer.analyze`、`classify_quote_roles` 和 `CjkFontRoleClassifier`，保留了 pair stack、
in-word apostrophe、unmatched directional context、paragraph language、nested pairs、Unicode script evidence 和
explainable decision source 的原始 API 边界。

Rust `TextRange` 与 Kotlin 一样使用 UTF-16；本批的 quote signature、`lastIndexOf` 及 debug index 断言均将
Rust UTF-8 byte offset 转换为 UTF-16 offset，避免中文或 supplementary scalar 使测试位置偏移。函数内部的
case matrix 完整保留 Kotlin 对应函数，未合并 Kotlin `@Test fun`。现有
`tests/layout/quote_pair_analyzer_test.rs` 的合并式 regression 不计入这批映射。

实际验证：新模块完整运行结果为 31 passed、0 failed。首次运行中曾加入 Kotlin 原函数不含的 pair-count 断言；
该断言与上游契约无关，已删除后重跑。命令仅报告既有
`src/layout/paragraph_dp_line_breaker.rs` 的 `gap_boundaries` dead-code warning。

#### T4 第三十一批 QuoteClassificationEngineTest 完整函数映射（2026-09-01）

`QuoteClassificationEngineTest.kt` 的 30 个 Kotlin `@Test fun` 已逐项迁移至
`tests/layout/quote_classification_engine_historical_coverage_test.rs`。每项通过
`ExplainableStubParagraphLayoutEngine` 的公开 layout pipeline 检查 font decision、role override、
punctuation geometry、source boundary、Lookahead line breaking 和 positioned geometry；没有以直接 resolver
测试代替原有 layout 测试。

proportional curly quote 的 shaper 是 Kotlin fake shaper 的字段级翻译：对 `“` 和 `”` 保留 `fwid=1` 检查、
6px advance 和同一 ink bounds。所有 debug index 与末端 quote 定位均按 UTF-16 offset 计算。既有
`tests/layout/quote_classification_engine_test.rs` 中合并式 regression 不计入这批逐函数映射。

实际验证：新模块完整运行结果为 30 passed、0 failed；编辑器诊断为零。命令仅报告既有
`src/layout/paragraph_dp_line_breaker.rs` 的 `gap_boundaries` dead-code warning。

#### T4 函数级审计完成（2026-09-01）

T4 锁定的 22 个 Kotlin 资产共含 451 个 `@Test fun`。其中 425 个可由 Rust 有效 UTF-8 `Text` 表达的函数均有
独立 Rust `#[test]`；18 个仅依赖孤立或非法 UTF-16 surrogate 的函数按 Rust 文本模型标为 N/A；
`NativeFontBackendTest.kt` 的 8 个 Kotlin/Native vtable、packed buffer 与 callback retry ABI 函数在 Rust core
不存在同构 ABI producer/consumer，按实施前映射标为 N/A。含有效 scalar 和非法 surrogate 子例的函数保留独立
Rust 测试，只有不可表达子例标为 N/A。

审计覆盖 paragraph shaping、annotation cache、cluster role、contextual dash/quote、Unicode punctuation、Emoji 17、
shaping contracts、font metrics、display glyph substitution、QuotePairAnalyzer 和 QuoteClassificationEngine。没有
剩余的 T4 Kotlin 文件或函数待映射；T5 与 P3 保持未开始。

最终验证：`cargo test --manifest-path /home/icemic/workspace/tiqian-rs/Cargo.toml` 为 1053 passed、0 failed；
doc-tests 为 0 passed、0 failed。`git diff --check` 与本批新增文件的 `git diff --check --no-index` 均通过。
`tools/verify-recorded-fixtures.sh` 没有执行权限，使用 `bash tools/verify-recorded-fixtures.sh` 按原脚本执行后，
52 个 Kotlin recorded fixture layout dump 均与 Rust replay golden 相同。上述 Rust 命令仅报告既有
`src/layout/paragraph_dp_line_breaker.rs` 的 `gap_boundaries` dead-code warning。

### T5：断行、repair、调整、hyphenation 与 progressive break

T5 输入由 `tools/list_kotlin_test_audit.py --group line-breaking-and-adjustment --paths` 锁定：35 个 Kotlin
资产，其中 `commonTest` 33 个、JVM-only probe 2 个。P3 的索引语义已迁移，剩余性能优化独立跟踪；T5 继续按函数
同步既有行为和可观察 decision。

#### T5 第一批：LineAdjustmentStage 覆盖与 JF（2026-09-01）

`LineAdjustmentStageCoverageTest.kt` 的 20 个与 `LineAdjustmentStageJfTest.kt` 的 5 个 Kotlin `@Test fun` 均已
逐项翻译为 `tests/layout/line_adjustment_stage_coverage_test.rs` 中独立的 Rust `#[test]`。保留 Kotlin 的
`noIndent`（零首行缩进、关闭 grid）、hyphenation、Taiwan profile、inline object、baseline shift、technical span
与 dash ink-bound fake shaper 输入；没有以既有相近测试、fixture 或循环替代函数级映射。

本批在
`LineAdjustmentStageCoverageTest.attachedObjectMarkHangsInsteadOfLeavingTheSeparatorAtAnEdge` 首次暴露有效输入的
生产分叉。Kotlin `LineRepair` 在 Hang 后重建剩余当前行时直接 `continue`，重检相同 line index，使附着对象后的
separator 和逗号连续加入同一 hanging suffix；Rust 额外 `i += 1`，跳过刚暴露的逗号。经用户确认后，
`src/layout/line_repair.rs` 删除该唯一递增语句，直接恢复 Kotlin 控制流。没有修改 P3 索引、标点 geometry 或
inline attachment 预处理。

实际验证：先运行
`layout::line_adjustment_stage_coverage_test::attached_object_mark_hangs_instead_of_leaving_the_separator_at_an_edge`，
结果为 1 passed、0 failed；随后完整模块为 25 passed、0 failed。编辑器诊断为零。Cargo 仅报告既有
`src/layout/paragraph_dp_line_breaker.rs` 的 `gap_boundaries` dead-code warning。

#### T5 第二批：LineRepair direct-stage coverage（2026-09-01）

`LineRepairCoverageTest.kt` 的 29 个与 `LineRepairTailCoverageTest.kt` 的 1 个 Kotlin `@Test fun` 均已逐项
翻译为新建的 `tests/layout/line_repair_coverage_test.rs` 中独立 Rust `#[test]`。测试保持 Kotlin 的手工
cluster advance、`LineCandidate` ranges、mandatory-break tail、shrink channel、protected hang group、fill-pull
bias 与 progressive tier 输入，直接调用 `apply_kinsoku_repairs`、`try_push_in`、`apply_fill_push_in` 和
`with_fill_push_in`；不以 Greedy 或 Lookahead 集成测试替代。

`pushInReportsInfinityCapacityWithAPortableDebugString` 发现 Rust `portable()` 使用标准库的 `inf`，而 Kotlin
`Float.toString()` 生成 `Infinity` 后照常追加 `.0`。经用户确认后，Rust 仅在该 formatter 中把正负无穷规范为
`Infinity.0` / `-Infinity.0`，使 `RepairOption::PushIn.reason` 与 Kotlin 结构化 debug 文本一致。

实际验证：`cargo test --test tiqian line_repair_coverage_test -- --test-threads=3 --nocapture` 为 30 passed、0 failed。
两条 merge-through 范围测试的 panic 均由 `#[should_panic]` 预期并通过。编辑器诊断为零；Cargo 仅报告既有
`src/layout/paragraph_dp_line_breaker.rs` 的 `gap_boundaries` dead-code warning。

#### T5 第三批：Greedy 与 Lookahead breaker（2026-09-01）

`GreedyLineBreakerTest.kt` 的 19 个 Kotlin `@Test fun` 已由
`tests/layout/greedy_line_breaker_test.rs` 的 19 个独立 Rust `#[test]` 覆盖；本批补齐 single cluster、single-line
LeaveRagged、custom kinsoku、misaligned list、hang disabled、PushIn-over-Hang、sole opener 与 mandatory-boundary。
`LookaheadLineBreakerTest.kt` 的 12 个 Kotlin 函数也已由
`tests/layout/lookahead_line_breaker_test.rs` 的 12 个独立 Rust `#[test]` 覆盖；本批补齐 hanging suffix invariant、
compatibility hanging index、PushIn candidate、ordinary fallback 与 consecutive synthetic-hyphen penalty。

测试保持 Kotlin 的 cluster advance、breaker window/horizon、repair penalty、hyphen boundary、unbreakable range 和
Kinsoku override；没有以相近 layout fixture 或 repair direct-stage 函数替代 breaker 函数。Kotlin 的
`assertFailsWith` 映射为同一 production invariant 的 `#[should_panic]`。

实际验证：Greedy 模块为 19 passed、0 failed；Lookahead 模块为 12 passed、0 failed。两条预期 panic 分别来自
natural/adjusted list 未对齐和非 trailing hanging suffix。编辑器诊断为零；Cargo 仅报告既有
`src/layout/paragraph_dp_line_breaker.rs` 的 `gap_boundaries` dead-code warning。

#### T5 第四批：整段 repair、technical break 与 Kinsoku cohesion（2026-09-01）

`LineBreakRepairEngineTest.kt` 的 20 个 Kotlin `@Test fun` 现由
`tests/layout/line_break_repair_engine_test.rs` 的 20 个独立 Rust `#[test]` 覆盖。映射保留 default/fixed
Kinsoku profile、grid 开关、`NoHyphenator` 与原始 custom hyphenator cut、progressive technical span 的 UTF-16 range，
以及 Greedy、Lookahead、Paragraph-DP 的同函数内 strategy loop。opaque Latin、URL、all-caps、forced hyphen、
current-line emergency 和 source-space adjustment 均通过 public paragraph pipeline 断言。

`KinsokuAndCohesionRepairEngineTest.kt` 的 12 个 Kotlin 函数同样由
`tests/layout/kinsoku_and_cohesion_repair_engine_test.rs` 的 12 个独立 Rust `#[test]` 覆盖。新增项保留
LeaveRagged、Latin word boundary、bibliographic numeric locator、ordinary numeric-token loop、None/Strict Kinsoku 及
PauseStops/Disabled hanging 对照；repair candidate 和 break opportunity 使用 Rust 同一结构化 debug 输出断言。

实际验证：`line_break_repair_engine_test` 为 20 passed、0 failed；
`kinsoku_and_cohesion_repair_engine_test` 为 12 passed、0 failed。编辑器诊断为零；Cargo 仅报告既有
`src/layout/paragraph_dp_line_breaker.rs` 的 `gap_boundaries` dead-code warning。

#### T5 第五批：Justifier JF direct-stage 边界（2026-09-01）

`JustifierJfTest.kt` 的 14 个 Kotlin `@Test fun` 已逐项翻译至新建并注册的
`tests/layout/justifier_jf_test.rs`。测试直接构造 `JustificationRequest`，保留 Kotlin 的 East Asian spacing edge、
FontRole、empty line range、no-stretch set、attached-inline virtual/virtual-sino-western boundary、preferred inline
object relation、CJK-Latin headroom 与两种 capacity allocation 情况；没有用 paragraph layout 或相近 Justifier test
替代 direct-stage 函数。

`compressSubnormalUnderflowShrinkZero` 的 Kotlin `Float` 字面量 $1e^{-300}f$ 和 $1e^{300}f$ 分别实际为
`0.0f` 和正无穷。Rust 拒绝溢出的 `f32` literal，故在测试中明确使用 `0.0` 与 `f32::INFINITY`；Kotlin 原函数已实际
运行并通过，Rust 输入与其实际 Float 值、early-exit 断言一致。

实际验证：Kotlin `JustifierJfTest.compressSubnormalUnderflowShrinkZero` 通过；Rust
`layout::justifier_jf_test::` 为 14 passed、0 failed，编辑器诊断为零。Cargo 仅报告既有
`src/layout/paragraph_dp_line_breaker.rs` 的 `gap_boundaries` dead-code warning。

#### T5 第六批：Justifier 基础与 public engine（2026-09-01）

`JustifierCompressionTest.kt` 的 5 个函数原已独立对应；`JustifierTest.kt` 的 14 个函数现由 14 个独立
Rust `#[test]` 覆盖。映射保留 inline object 的 uniform/preferred tier 次序、mixed CJK punctuation-Western boundary、
word 与 CJK-Latin gap 的 final uniform、western bracket、attached virtual prose edge、slash-led Latin run exclusion 和
$1/3$ em cap。

`JustifierEngineTest.kt` 的 16 个函数现由 `tests/layout/justifier_engine_test.rs` 中同名 snake_case 的 16 个独立
Rust `#[test]` 全部覆盖。新增映射保留 Kotlin 的 priority-chain allocation、CJK inter-character final fallback、
bracket interior、dash boundary、typed Sino-Western space、punctuation-Western edge、line-edge collapsed space，以及
`PositionedPairShaper` 的两个 Latin glyph origin。所有 16 项均走 `PushOutOnly` public paragraph pipeline。

`JustifierCoverageTest.kt` 实际含 29 个 Kotlin `@Test fun`，现由新建并注册的
`tests/layout/justifier_coverage_test.rs` 中 29 个独立 Rust `#[test]` 逐项覆盖。测试直接构造
`JustificationRequest`，保持 Kotlin 的手工 cluster advance、FontRole、East Asian spacing edge、所有 no-stretch
disjunct、technical/word/Sino-Western tier、attached-inline virtual edge、preferred/uniform object、Western gate、
emergency tracking、empty range、paragraph edge space及两项 compression 分配。Kotlin `List.single()` 在 Rust 中以
单元素 slice 模式匹配翻译，继续断言 allocation 数量恰为一。

实际验证：`layout::justifier_test::` 为 14 passed、0 failed；
`layout::justifier_engine_test::` 为 19 passed、0 failed，其中 16 项为 Kotlin 映射、3 项为既有 Rust 回归；
`layout::justifier_coverage_test::` 为 29 passed、0 failed；`layout::justifier_jf_test::` 为 14 passed、0 failed。
Coverage misalignment 函数的两条 panic 均由 `catch_unwind` 预期捕获。编辑器诊断为零；Cargo 仅报告既有
`src/layout/paragraph_dp_line_breaker.rs` 的 `gap_boundaries` dead-code warning。

#### T5 第七批：Progressive decision 与 technical break（2026-09-01）

`ProgressiveBreakDecisionsCoverageTest.kt` 的 17 个 Kotlin `@Test fun` 已逐项翻译至新建并注册的
`tests/layout/progressive_break_decisions_coverage_test.rs`。每个 Rust `#[test]` 直接调用
`decide_progressive_break`、`progressive_candidate_allowed` 或 `decide_hyphen_break`，保留 Kotlin 的 optional
geometry default、empty scan、rightmost same-tier、density estimate、span/map candidate guard、hyphen word boundary 与
Sino-Western capacity 输入。

`ProgressiveBreakDecisionsTailTest.kt` 的 2 个函数已由
`tests/layout/progressive_break_decisions_tail_test.rs` 中独立 Rust `#[test]` 对应，覆盖 infinite line limit 与
infinite CJK stretch ceiling 的两个 guard direction。既有 `ProgressiveTechnicalBreakTest.kt` 的 2 个函数与
`tests/layout/progressive_technical_break_test.rs` 的两个独立测试一对一对应。

实际验证：`layout::progressive_break_decisions_coverage_test::` 为 17 passed、0 failed；
`layout::progressive_break_decisions_tail_test::` 为 2 passed、0 failed；
`layout::progressive_technical_break_test::` 为 2 passed、0 failed。编辑器诊断为零；Cargo 仅报告既有
`src/layout/paragraph_dp_line_breaker.rs` 的 `gap_boundaries` dead-code warning。

#### T5 第八批：Line-break planning stage coverage（2026-09-01）

`LineBreakPlanningStageCoverageTest.kt` 的 6 个 Kotlin `@Test fun` 已逐项翻译至新建并注册的
`tests/layout/line_break_planning_stage_coverage_test.rs`。测试保留 Kotlin 的 real-paragraph-1 fixture 文本、320px
constraint、Lookahead breaker 与 PushInFirst/PushOutFirst policy 对比，以及 explicit zero line-height、zero-width/
mandatory control、inline object、technical punctuation 与 overlapping technical span 输入。

`LineBreakPlanningStageCoverage2Test.kt` 的 8 个函数已逐项翻译至
`tests/layout/line_break_planning_stage_coverage2_test.rs`。每个 direct-stage 函数先通过 Rust 的真实
`prepare_width_independent_annotation` 和 `build_paragraph_layout_prep` 建立 prep，再仅覆盖 Kotlin 指定的公开字段，
并直接调用 `plan_paragraph_lines`。由此保持 font-decision crossing、empty metric face-selection text、unmapped
progressive offset、emergency eligibility、empty/whitespace boundary 与 adjustable object boundary 的原始测试层级。

实际验证：`layout::line_break_planning_stage_coverage_test::` 为 6 passed、0 failed；
`layout::line_break_planning_stage_coverage2_test::` 为 8 passed、0 failed。后者的
`testClusterCrossesFontDecisionThrows` panic 由 `#[should_panic]` 预期。编辑器诊断为零；Cargo 仅报告既有
`src/layout/paragraph_dp_line_breaker.rs` 的 `gap_boundaries` dead-code warning。

#### T5 第九批：Line breaker 与 candidate validation（2026-09-01）

`LineBreakerCoverage2Test.kt` 的 9 个 Kotlin `@Test fun` 已逐项翻译至新建并注册的
`tests/layout/line_breaker_coverage2_test.rs`。测试直接调用 Rust `LineBreaker`、`LookaheadLineBreaker`、
`LineBreakerConfig` 和 line helper，保留 trait strategy-name default、natural/adjusted mismatch、negative window/
horizon、non-rendering control、hard break terminal empty line、progressive ending、gap count、empty rebuild、default
greedy end 与 orphan/synthetic hyphen run 输入。

`LineCandidateValidationTest.kt` 的 6 个函数已逐项翻译至
`tests/layout/line_candidate_validation_test.rs`。测试先以 Rust `LineCandidate::new` 建立同一 candidate，再设置
hanging suffix 并立即调用 `validate_hanging_suffix`，从而保留 Kotlin 构造期 invariant 的层级；Rust 的 `IntRange` /
`HashSet` debug 文本与 Kotlin 类型不同，首个函数断言实际 Rust message，其余 rejection 保持同一 invariant 的
`#[should_panic]`。

实际验证：`layout::line_breaker_coverage2_test::` 为 9 passed、0 failed；
`layout::line_candidate_validation_test::` 为 6 passed、0 failed。Lookahead precondition、empty rebuild 和 hanging
suffix panic 均为 Kotlin `assertFailsWith` 的预期路径。编辑器诊断为零；Cargo 仅报告既有
`src/layout/paragraph_dp_line_breaker.rs` 的 `gap_boundaries` dead-code warning。

#### T5 第十批：Paragraph-DP line breaker（2026-09-02）

`ParagraphDpLineBreakerTest.kt` 的 10 个 Kotlin `@Test fun` 已与
`tests/layout/paragraph_dp_line_breaker_test.rs` 的 10 个独立 Rust `#[test]` 对齐。原 Rust 文件将 Kotlin 的
`compressionEdgeRecordsPushInRepair` 与 `compressionDisabledWithoutPushInFlag` 合并在同一函数；现已拆为两个
独立测试，并补入 `compressedSameTierBoundaryIsNotReportedAsPromotion`。其 helper 同步 Kotlin 默认的全 CJK gap
集合与 8px gap stretch cap；same-tier 测试按 Kotlin 显式传入空集合。

`ParagraphDpLineBreakerCoverageTest.kt` 的 12 个函数已逐项翻译至
`tests/layout/paragraph_dp_line_breaker_coverage_test.rs`，覆盖 empty/mismatch/window guards、shrink prefix、
line-end-only compression、segment-end compression、mandatory commit、tier promotion、null resulting opportunity、
mandatory candidate boundary、narrow candidate window 与 trait default strategy name。

`ParagraphDpLineBreakerCoverage2Test.kt` 的 7 个函数已逐项翻译至
`tests/layout/paragraph_dp_line_breaker_coverage2_test.rs`。其中
`testTierPreferredPoolEmptyFallback` 对应独立 Rust 测试
`test_tier_preferred_pool_empty_fallback`：使用 `UnbreakableRanges(0..3)` 使所有候选均被过滤，固定 DP 走 fallback 后仍返回非空解，
不以相似 DP 用例替代。

`ParagraphDpTierPromotionPoolTest.kt` 的 3 个函数已逐项翻译至
`tests/layout/paragraph_dp_tier_promotion_pool_test.rs`，保留 foreign-span pool candidate、foreign-span commit 与
missing resulting opportunity 的 PushIn reason 判定。

实际验证：`layout::paragraph_dp_line_breaker_coverage_test::` 前 9 项为 9 passed、0 failed，后 3 项分别为
1 passed、0 failed；`layout::paragraph_dp_line_breaker_coverage2_test::` 原有 6 项为 6 passed、0 failed，2026-09-02
新增 `test_tier_preferred_pool_empty_fallback` 定向运行结果为 1 passed、0 failed；
`layout::paragraph_dp_tier_promotion_pool_test::` 为 3 passed、0 failed；
`layout::paragraph_dp_line_breaker_test::` 为 10 passed、0 failed。编辑器诊断为零；Cargo 仅报告既有
`src/layout/paragraph_dp_line_breaker.rs` 的 `gap_boundaries` dead-code warning。

#### T5 第十一批：Line optimization model（2026-09-02）

`LineOptimizationCoverageTest.kt` 的 11 个 Kotlin `@Test fun` 已逐项翻译至新建并注册的
`tests/layout/line_optimization_coverage_test.rs`。覆盖 BreakCandidate 默认值与显式 repair、hanging suffix 的
rejection/acceptance/in-measure view、Hang offender priority、CarryNext、RepairCandidate 默认值、LineSolution 默认
badness 与三种 optimization strategy。Kotlin data-class 默认参数在 Rust 中使用同字段 struct literal 或公开
`new` constructor；strategy `entries` 按三个公开 enum variant 分别断言，不引入 Rust 反射。

实际验证：`layout::line_optimization_coverage_test::` 为 11 passed、0 failed。hanging suffix 的四条 panic 均是
Kotlin `assertFailsWith` 的预期子构造路径。编辑器诊断为零；Cargo 仅报告既有
`src/layout/paragraph_dp_line_breaker.rs` 的 `gap_boundaries` dead-code warning。

#### T5 第十二批：Hyphenation 与 paragraph input validation（2026-09-02）

`DecideHyphenBreakTest.kt` 的 2 个函数已确认由注册的
`tests/layout/decide_hyphen_break_test.rs` 逐函数对应；CJK deficit 与 Sino-Western capacity 的 clusters、line limit、
boundary sets 与 caps 均逐项相同。`HyphenationLayoutTest.kt` 的 6 个函数已确认由注册的
`tests/layout/hyphenation_layout_test.rs` 逐函数对应：默认 en-US、显式 NoHyphenator/en-US、PushOutOnly、grid off、
syllable reconstruction、comma glue compression 与 measure-reserved hyphen 均保留 Kotlin 输入与断言。两文件无需新增
重复测试。

`ParagraphLayoutEngineValidationCoverageTest.kt` 的 13 个函数中，12 个有效 UTF-8 输入守卫已逐项翻译至新建并注册的
`tests/layout/paragraph_layout_engine_validation_coverage_test.rs`。它们覆盖 paragraph style finite/non-negative、
inline box range/edge、line-break/auto-space range、inline object range uniqueness/overlap/geometry/boundary。
`sourceTextMustNotContainUnpairedSurrogates` 不适用：Kotlin 在运行时可构造非法 UTF-16 code unit 序列，Rust `Text`
只表示有效 UTF-8，无法且不应伪造同一输入；有效 supplementary scalar 路径另有 Rust 覆盖。

实际验证：`layout::paragraph_layout_engine_validation_coverage_test::` 为 12 passed、0 failed。所有显示的 panic 都是
Kotlin `assertFailsWith` 的预期守卫分支。编辑器诊断为零；Cargo 仅报告既有
`src/layout/paragraph_dp_line_breaker.rs` 的 `gap_boundaries` dead-code warning。

#### T5 第十三批：Emergency grapheme tracking（2026-09-02）

`EmergencyGraphemeTrackingTest.kt` 的 10 个 Kotlin `@Test fun` 现由
`tests/layout/emergency_grapheme_tracking_test.rs` 的 10 个独立 Rust `#[test]` 覆盖。此前已有 5 个函数级映射；
本批补齐 technical identifier 的 uniform-advance shaper、repeated plain token、inline object/zero-width control edge、
unannotated URL 与 ordinary Western prose。所有测试走公开 paragraph pipeline；uniform shaper 保留 Kotlin 的单 cluster
和每 UTF-16 code unit 10px advance，inline object 及 U+200B 分支保留同一 ProgressiveTechnical span 与 300px measure。

实际验证：`layout::emergency_grapheme_tracking_test::` 为 10 passed、0 failed。编辑器诊断为零；Cargo 仅报告既有
`src/layout/paragraph_dp_line_breaker.rs` 的 `gap_boundaries` dead-code warning。

#### T5 第十四批：Explainable stub paragraph layout engine（2026-09-02）

`ExplainableStubParagraphLayoutEngineTest.kt` 的 18 个 Kotlin `@Test fun` 现由
`tests/layout/explainable_stub_paragraph_layout_engine_test.rs` 的 18 个独立 Rust `#[test]` 覆盖。此前合并的
mandatory-break、emoji 和 source-boundary 场景已按 Kotlin 函数边界拆开；每项继续走 public paragraph pipeline，保留
Lookahead injection、mandatory/CRLF/blank line、justification skip、shaper coverage rejection、glyph bounds、fallback、
combining mark、emoji sequence/role promotion、source grapheme boundary 及 text-span style boundary 的原始断言层级。

实际验证：`layout::explainable_stub_paragraph_layout_engine_test::` 为 18 passed、0 failed。shaper coverage panic 是
Kotlin `assertFailsWith` 对应的 production invariant。编辑器诊断为零；Cargo 仅报告既有
`src/layout/paragraph_dp_line_breaker.rs` 的 `gap_boundaries` dead-code warning。

#### T5 函数级审计完成（2026-09-02）

33 个 Kotlin `commonTest` 资产的函数级映射已完成；每个可表示的 Kotlin `@Test fun` 均由独立 Rust `#[test]`
覆盖。`ParagraphDpLineBreakerCoverage2Test.testTierPreferredPoolEmptyFallback` 已在 P3 索引迁移后补为独立回归并
通过定向验证。

两个 JVM-only 输入文件已逐函数处置。`ParagraphDpReferenceExperiment` 的
`compareLookaheadAgainstParagraphDp`、`productionDpBeatsLookaheadOnCommittedOutput` 与
`benchmarkDpAgainstLookahead` 是上游算法实验，按决定不迁移，详见 `docs/key-differences.md` 的“其他”章节。
`ParagraphDpTuningProbe.reportPerceptualProfiles` 和 `sweepCompressionVisibility` 依赖 JVM `AwtTextShaper` 与宿主
字体 advance；Rust 没有同源后端，标为不适用，不能以 stub 输出替代。

T5 覆盖 Greedy、Lookahead、Paragraph-DP、repair、push-in、justification、hyphenation、progressive break、zero-width
control、planning stage、paragraph input validation 与 public paragraph pipeline。所有普通测试函数已完成映射或有明确的
JVM-only 处置；P3 的 80k giant-token 性能验证已完成。

最终验证：`cargo test --release --test tiqian justifier` 为 81 passed、0 failed；
`giant_token_layout_stays_far_below_the_quadratic_ceiling` 为 425.711ms，通过；
`measure_giant_token_scaling_matrix` 在 `TIQIAN_RUN_EXPERIMENTS=1` 下通过。`git diff --check` 待本轮收尾后执行。



